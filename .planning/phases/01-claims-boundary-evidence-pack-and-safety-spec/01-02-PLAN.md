---
phase: 01-claims-boundary-evidence-pack-and-safety-spec
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - docs/safety/a1c-band-rubric.md
autonomous: true
requirements:
  - INPUT-04
  - INPUT-05
  - GUIDE-02
user_setup: []
must_haves:
  truths:
    - "A1C values below 5.7 route to out-of-scope prediabetes-only guidance instead of SAFE, MODERATE, or HIGH."
    - "A1C values from 5.7 through 6.4 are grouped into explicit bands 5.7-5.9, 6.0-6.2, and 6.3-6.4."
    - "A1C values of 6.5 or above route to clinician/RD guidance without diagnosing the user."
    - "Higher A1C bands increase conservative calibration without pretending to personalize exact physiology."
  artifacts:
    - path: "docs/safety/a1c-band-rubric.md"
      provides: "Deterministic A1C routing table, in-scope band rubric, and out-of-range handling rules"
      contains: "5.7-5.9"
    - path: "tests/fixtures/safety-contract.json"
      provides: "A1C route fixtures consumed by the validator"
      contains: "a1cRoutes"
    - path: "scripts/validate-safety-contract.mjs"
      provides: "Static validation for A1C routes"
      contains: "a1c-routes"
  key_links:
    - from: "docs/safety/a1c-band-rubric.md"
      to: "tests/fixtures/safety-contract.json"
      via: "The human-readable route table uses the same band ids and response kinds as fixture examples"
      pattern: "prediabetes_57_59"
    - from: "scripts/validate-safety-contract.mjs"
      to: "docs/safety/a1c-band-rubric.md"
      via: "`--a1c-routes` verifies required bands and out-of-scope routes"
      pattern: "a1c-band-rubric.md"
---

<objective>
Define Revora's deterministic A1C band rubric and out-of-range handling rules.

Purpose: Later inference and UI work must not ask the model to decide whether an A1C value is in scope.
Output: A locked A1C routing spec that handles below-range, three prediabetes bands, and 6.5+ guidance conservatively.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-RESEARCH.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-VALIDATION.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-01-SUMMARY.md
@docs/safety/claims-boundary.md
@docs/safety/a1c-band-rubric.md
@tests/fixtures/safety-contract.json
@scripts/validate-safety-contract.mjs

<interfaces>
Plan 01-01 creates the validator and fixture. Preserve those contracts:

- Run `node scripts/validate-safety-contract.mjs --a1c-routes` after each task.
- Keep route ids aligned with `tests/fixtures/safety-contract.json`: `below_prediabetes_range`, `prediabetes_57_59`, `prediabetes_60_62`, `prediabetes_63_64`, and `diabetes_range_out_of_scope`.
- Out-of-range routes are not result classifications. They must not return SAFE, MODERATE, or HIGH.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define deterministic A1C routes</name>
  <files>docs/safety/a1c-band-rubric.md</files>
  <action>Replace the seeded rubric with a complete route table. Use these exact bands and boundaries: below `5.7` is `below_prediabetes_range` with `responseKind: out_of_scope_below`; `5.7` inclusive to below `6.0` is `prediabetes_57_59`; `6.0` inclusive to below `6.3` is `prediabetes_60_62`; `6.3` inclusive to below `6.5` is `prediabetes_63_64`; `6.5` and above is `diabetes_range_out_of_scope` with `responseKind: out_of_scope_high`. Document that these are scope routes, not diagnoses. For below `5.7`, explain Revora is designed for the prediabetes A1C range and should not classify the meal. For `6.5+`, explain the value is in the range clinicians use when evaluating Type 2 diabetes and direct the user to a doctor or registered dietitian without saying the user has diabetes.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs --a1c-routes</automated>
  </verify>
  <done>The rubric contains all five route ids, the three required prediabetes bands, and safe out-of-scope copy for below-range and high-range inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Calibrate band conservatism without false precision</name>
  <files>docs/safety/a1c-band-rubric.md</files>
  <action>Add a calibration section explaining how guidance becomes more conservative across `5.7-5.9`, `6.0-6.2`, and `6.3-6.4`. Use qualitative levels such as standard, elevated, and high conservatism. Document conservative floors: upper-band A1C plus uncertain carb-containing meals cannot return SAFE; clearly low-impact foods may still return SAFE with permission-first reassurance; borderline foods should move to MODERATE rather than reassuring SAFE. Explicitly ban exact physiology assumptions such as predicted mg/dL changes, future A1C changes, or treating `6.1` vs `6.2` as precise personalization. Include at least one example row per band showing how the same borderline food becomes no more reassuring as A1C increases.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs --a1c-routes</automated>
    <automated>node scripts/validate-safety-contract.mjs</automated>
  </verify>
  <done>A1C bands have increasing qualitative conservatism, examples are documented, out-of-range values never classify food, and the full safety validator passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/validate-safety-contract.mjs --a1c-routes` passes.
- [ ] `node scripts/validate-safety-contract.mjs` passes.
- [ ] `docs/safety/a1c-band-rubric.md` contains below-range, all three prediabetes bands, and 6.5+ out-of-scope handling.
- [ ] No implementation files, package installs, or external services were added.
</verification>

<success_criteria>
- A1C below `5.7` routes to prediabetes-only out-of-scope guidance.
- A1C `5.7-5.9`, `6.0-6.2`, and `6.3-6.4` are explicit in-scope bands.
- A1C `6.5+` routes to clinician/RD guidance without diagnosis.
- The rubric uses qualitative conservative calibration only.
- `node scripts/validate-safety-contract.mjs` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-02-SUMMARY.md`
</output>
