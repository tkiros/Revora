---
phase: 05-community-launch-and-founder-review-loop
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - docs/validation/founder-review-loop.md
  - docs/ops/incident-feedback-loop.md
  - docs/validation/scanner-next-gate.md
  - lib/revora/review-record.ts
  - lib/revora/scanner-gate.ts
  - scripts/validate-community-launch.mjs
  - tests/unit/revora/review-record.test.ts
  - tests/unit/revora/scanner-gate.test.ts
autonomous: true
requirements:
  - GUARD-07
  - VALID-03
  - VALID-05
  - VALID-06
must_haves:
  truths:
    - "The founder can manually review the first 50 production results without default storage of raw production inputs or outputs."
    - "The founder can spot-check at least 5 results per day for the first two weeks after launch."
    - "S0 and S1 incidents create eval-backfill work and tie directly to Phase 4 kill-switch or rollback decisions."
    - "The scanner-next gate is deterministic and earned only by 3 of 5 WTP yeses at $5/month or 10+ verified organic shares within two weeks."
    - "If the scanner gate is not earned, scanner, native mobile, auth, saved history, payments, Stripe, and broader product expansion remain deferred."
  artifacts:
    - path: "docs/validation/founder-review-loop.md"
      provides: "First-50 review checklist, daily spot-check schedule, redacted evidence policy, and completion status"
      contains: "first 50"
    - path: "lib/revora/review-record.ts"
      provides: "Redacted review record schema plus incident severity helpers"
      exports: ["ReviewRecord", "validateReviewRecord", "incidentRequiresRollback", "incidentRequiresEvalBackfill"]
    - path: "docs/ops/incident-feedback-loop.md"
      provides: "Severity rubric, eval-backfill requirements, and Phase 4 kill-switch/rollback linkage"
      contains: "public_checks_enabled"
    - path: "docs/validation/scanner-next-gate.md"
      provides: "Signed post-launch go/no-go decision record with WTP and organic-share counts"
      contains: "$5/month"
    - path: "lib/revora/scanner-gate.ts"
      provides: "Deterministic VALID-06 gate evaluator"
      exports: ["evaluateScannerGate"]
    - path: "tests/unit/revora/review-record.test.ts"
      provides: "No-raw-field, review-schedule, and incident-backfill coverage"
      contains: "raw food"
    - path: "tests/unit/revora/scanner-gate.test.ts"
      provides: "Threshold coverage for WTP and verified organic shares"
      contains: "share_intent"
  key_links:
    - from: "docs/validation/founder-review-loop.md"
      to: "lib/revora/review-record.ts"
      via: "Review docs and schema share the same redacted fields and first-50/daily review requirements"
      pattern: "ReviewRecord"
    - from: "docs/ops/incident-feedback-loop.md"
      to: "docs/ops/launch-controls.md"
      via: "S0 incidents require Phase 4 `public_checks_enabled=false` pause before reopening"
      pattern: "public_checks_enabled"
    - from: "docs/ops/incident-feedback-loop.md"
      to: "tests/unit/revora/review-record.test.ts"
      via: "Every S0 or S1 incident requires eval backfill or an explicit not-needed reason"
      pattern: "evalBackfilled"
    - from: "docs/validation/scanner-next-gate.md"
      to: "docs/validation/demand-ledger.md"
      via: "Scanner decision reads WTP yeses and verified organic shares from the redacted demand ledger, not share-intent telemetry"
      pattern: "verified_organic_share"
    - from: "lib/revora/scanner-gate.ts"
      to: "tests/unit/revora/scanner-gate.test.ts"
      via: "The deterministic gate is covered for earned and not-earned cases"
      pattern: "evaluateScannerGate"
---

<objective>
Run the founder review loop and define the post-launch go/no-go gate.

Purpose: Revora can only learn safely after launch if founder review, real incident backfill, rollback decisions, and expansion gates are explicit before community demand data arrives.
Output: Redacted review schema, first-50 and daily spot-check workflow, incident feedback loop tied to evals and Phase 4 rollback controls, deterministic scanner-next evaluator, scanner decision record, and tests/static validation for all of them.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-community-launch-and-founder-review-loop/05-RESEARCH.md
@.planning/phases/05-community-launch-and-founder-review-loop/05-VALIDATION.md
@.planning/phases/05-community-launch-and-founder-review-loop/05-01-PLAN.md
@.planning/phases/04-privacy-minimal-launch-controls/04-01-PLAN.md
@.planning/phases/04-privacy-minimal-launch-controls/04-02-PLAN.md

<interfaces>
This plan depends on Plan 05-01 and the earlier Phase 4 privacy/rollback contracts. Before any edits, verify the files below. If any are missing, stop and report `BLOCKED_PLAN_DEPENDENCY`; do not recreate Phase 2, Phase 3, Phase 4, or Plan 05-01 artifacts.

Required dependency gate:

```text
package.json
vitest.config.ts
playwright.config.ts
app/page.tsx
app/api/check/route.ts
app/api/health/route.ts
lib/revora/service.ts
lib/revora/openai-client.ts
lib/revora/telemetry.ts
lib/revora/env.ts
lib/revora/launch-controls.ts
docs/privacy/data-flow.md
docs/ops/launch-controls.md
docs/launch/community-rules-snapshot.md
docs/launch/modmail-request.md
docs/launch/community-post.md
docs/validation/demand-ledger.md
lib/revora/launch-metrics.ts
scripts/validate-community-launch.mjs
tests/unit/revora/launch-copy.test.ts
tests/unit/revora/launch-metrics.test.ts
```

Preserve the Phase 4 privacy boundary and Plan 05-01 demand evidence boundary. Do not add raw production log storage, saved history, auth, database-backed user profiles, scanner, native mobile, payments, Stripe, CRM, rich analytics, or any durable field for raw food, raw A1C, prompt text, full model output, IP, user agent, username, private DM links, or contact info.

Create this review record contract:

```typescript
export type ReviewRecord = {
  reviewId: string;
  source: "first_50" | "daily_spot_check" | "incident_followup";
  createdDay: string;
  a1cBand: "5.7-5.9" | "6.0-6.2" | "6.3-6.4" | "out_of_scope" | "not_recorded";
  responseKind: "result" | "clarify" | "not_food" | "out_of_scope" | "retry";
  risk?: "SAFE" | "MODERATE" | "HIGH";
  coarseFoodCategory?: "mixed_meal" | "grain_starch" | "dessert" | "beverage" | "protein" | "vegetable" | "unknown";
  disclaimerPresent: boolean;
  prohibitedClaimFlags: string[];
  reviewerVerdict: "pass" | "needs_eval" | "incident";
  incidentSeverity?: "S0" | "S1" | "S2";
  evalBackfilled?: boolean;
  evalBackfillNotNeededReason?: string;
};
```

Create this scanner gate contract:

```typescript
export type ScannerGateInput = {
  daysSinceLaunch: number;
  directWtpConversations: number;
  directWtpYesesAtFiveDollars: number;
  verifiedOrganicSharesWithinTwoWeeks: number;
  shareIntentClicks?: number;
};

export function evaluateScannerGate(input: ScannerGateInput): {
  scannerNextEarned: boolean;
  reason: "wtp_gate" | "organic_share_gate" | "not_earned";
};
```

`shareIntentClicks` must never satisfy the gate. Use verified organic shares from `docs/validation/demand-ledger.md` only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Verify dependencies and create the redacted founder review loop</name>
  <files>docs/validation/founder-review-loop.md, lib/revora/review-record.ts, scripts/validate-community-launch.mjs, tests/unit/revora/review-record.test.ts</files>
  <behavior>
    - Test 1: The task stops with `BLOCKED_PLAN_DEPENDENCY` if Phase 4 or Plan 05-01 artifacts are missing.
    - Test 2: Review records support first-50 review, daily spot checks, and incident follow-up without raw food, exact raw A1C, prompt text, full model output, username, private link, IP, user agent, or contact fields.
    - Test 3: `docs/validation/founder-review-loop.md` contains 50 first-result slots and a 14-day schedule with at least 5 spot checks per day.
    - Test 4: Review records require disclaimer presence and prohibited-claim flags so safety drift can be reviewed without raw transcripts.
    - Test 5: If exact reproduction is needed, the docs require a sanitized eval fixture or consented incident report instead of default raw production logging.
  </behavior>
  <action>Run the dependency gate from the interface block before creating or editing files. If it fails, stop and write `BLOCKED_PLAN_DEPENDENCY` in the summary. If it passes, write `tests/unit/revora/review-record.test.ts` first for the redacted schema and forbidden raw fields. Implement `lib/revora/review-record.ts` with the review record contract, a validation helper, and constants for allowed response kinds, risk classes, coarse food categories, and incident severities. Create `docs/validation/founder-review-loop.md` with 50 first-result review rows, a 14-day daily spot-check schedule requiring at least 5 results per day, review verdict definitions, severity placeholders, and a rule that exact reproduction uses sanitized eval fixtures or explicit consent rather than raw production logs. Extend `scripts/validate-community-launch.mjs` with `founder-review` checks for schedule coverage, redacted fields, and completion slots.</action>
  <verify>
    <automated>test -f package.json && test -f vitest.config.ts && test -f playwright.config.ts && test -f app/page.tsx && test -f app/api/check/route.ts && test -f app/api/health/route.ts && test -f lib/revora/service.ts && test -f lib/revora/openai-client.ts && test -f lib/revora/telemetry.ts && test -f lib/revora/env.ts && test -f lib/revora/launch-controls.ts && test -f docs/privacy/data-flow.md && test -f docs/ops/launch-controls.md && test -f docs/launch/community-rules-snapshot.md && test -f docs/launch/modmail-request.md && test -f docs/launch/community-post.md && test -f docs/validation/demand-ledger.md && test -f lib/revora/launch-metrics.ts && test -f scripts/validate-community-launch.mjs && test -f tests/unit/revora/launch-copy.test.ts && test -f tests/unit/revora/launch-metrics.test.ts && node scripts/validate-community-launch.mjs --section founder-review && npx vitest run tests/unit/revora/review-record.test.ts -x</automated>
    <automated>rg -n "first 50|5 results per day|14 days|sanitized eval fixture|consent" docs/validation/founder-review-loop.md && ! rg -n "rawFood|rawA1c|promptText|fullModelOutput|username|privateDm|contactInfo|ip|userAgent" lib/revora/review-record.ts docs/validation/founder-review-loop.md</automated>
    <manual>Founder must complete the first-50 review rows and daily spot-check rows after launch; empty evidence slots are allowed before traffic but not before claiming `GUARD-07` complete.</manual>
  </verify>
  <done>The founder review loop is executable and redacted, with clear evidence slots for first-50 and two-week daily spot-check completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Tie incidents to eval backfill and Phase 4 rollback decisions</name>
  <files>docs/ops/incident-feedback-loop.md, docs/validation/founder-review-loop.md, lib/revora/review-record.ts, scripts/validate-community-launch.mjs, tests/unit/revora/review-record.test.ts</files>
  <behavior>
    - Test 1: S0 incidents cover harmful SAFE, direct medical advice, treatment/reversal claims, missing disclaimer on terminal results, and require immediate Phase 4 pause.
    - Test 2: S0 and S1 incidents require eval backfill or an explicit not-needed reason before reopening normal launch operation.
    - Test 3: S0 incidents map to `public_checks_enabled=false`, Phase 4 rollback or kill-switch evidence, incident record, eval fixture, and rerun of the safety suite before restore.
    - Test 4: S1 incidents require prompt/copy/policy or eval patching plus 10 additional spot checks.
    - Test 5: S2 incidents can be fixed in normal flow but still require redacted incident notes.
  </behavior>
  <action>Extend `tests/unit/revora/review-record.test.ts` first for incident severity behavior. Update `lib/revora/review-record.ts` with helpers such as `incidentRequiresRollback()` and `incidentRequiresEvalBackfill()`. Create `docs/ops/incident-feedback-loop.md` with an S0/S1/S2 severity rubric, required actions, evidence fields, eval-backfill status, and explicit links to Phase 4 `docs/ops/launch-controls.md` for `public_checks_enabled=false`, `vercel rollback`, `/api/health`, and post-rollback synthetic check evidence. Update `docs/validation/founder-review-loop.md` so review rows can link to incident records and eval backfill. Extend `scripts/validate-community-launch.mjs` with `incident-feedback` checks. Do not store raw production transcripts; if an incident needs exact text, convert the minimum sanitized scenario into the Phase 2 eval suite or use a consented incident report before capturing exact details.</action>
  <verify>
    <automated>node scripts/validate-community-launch.mjs --section incident-feedback --section founder-review && npx vitest run tests/unit/revora/review-record.test.ts -t "incident|rollback|eval backfill" -x</automated>
    <automated>rg -n "S0|S1|S2|public_checks_enabled=false|vercel rollback|/api/health|evalBackfilled|10 additional spot checks" docs/ops/incident-feedback-loop.md docs/validation/founder-review-loop.md lib/revora/review-record.ts</automated>
    <manual>S0 or S1 incidents discovered after launch must have linked eval-backfill evidence before public checks are considered safe to restore.</manual>
  </verify>
  <done>Real launch incidents now feed safety eval backfill and Phase 4 kill-switch or rollback decisions instead of remaining freeform notes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the deterministic scanner-next gate and decision record</name>
  <files>docs/validation/scanner-next-gate.md, lib/revora/scanner-gate.ts, scripts/validate-community-launch.mjs, tests/unit/revora/scanner-gate.test.ts</files>
  <behavior>
    - Test 1: The gate returns earned by WTP only when there are at least 5 direct WTP conversations and at least 3 yeses to `$5/month`.
    - Test 2: The gate returns earned by shares only when there are at least 10 verified organic shares within two weeks.
    - Test 3: Share-intent clicks never satisfy the organic-share gate.
    - Test 4: If neither threshold is met, the decision record says scanner, native mobile, auth, saved history, payments, Stripe, and broader expansion remain deferred.
    - Test 5: `docs/validation/scanner-next-gate.md` requires launch date, two-week window, counts pulled from the redacted demand ledger, final decision, rationale, and founder sign-off.
  </behavior>
  <action>Write `tests/unit/revora/scanner-gate.test.ts` first with earned and not-earned cases, including a case where many `shareIntentClicks` still does not earn scanner-next. Implement `lib/revora/scanner-gate.ts` with the deterministic contract from the interface block: earned by WTP if `directWtpConversations >= 5` and `directWtpYesesAtFiveDollars >= 3`; earned by organic shares if `daysSinceLaunch <= 14` and `verifiedOrganicSharesWithinTwoWeeks >= 10`; otherwise not earned. Create `docs/validation/scanner-next-gate.md` as the signed decision record, explicitly sourcing WTP and verified organic-share counts from `docs/validation/demand-ledger.md`, excluding share-intent clicks, and keeping scanner/native/auth/history/payments deferred unless a threshold is met. Extend `scripts/validate-community-launch.mjs` with `scanner-gate` checks.</action>
  <verify>
    <automated>node scripts/validate-community-launch.mjs --section scanner-gate && npx vitest run tests/unit/revora/scanner-gate.test.ts -x</automated>
    <automated>rg -n "3 of 5|\\$5/month|10\\+ organic shares|share-intent|not_earned|scanner|native mobile|auth|saved history|payments" docs/validation/scanner-next-gate.md lib/revora/scanner-gate.ts tests/unit/revora/scanner-gate.test.ts</automated>
    <manual>Founder must fill the final scanner decision after two weeks or after the WTP threshold is reached; anecdotal interest is not enough.</manual>
  </verify>
  <done>The scanner-next decision is deterministic, evidence-gated, and impossible to satisfy using share-intent telemetry or founder enthusiasm alone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The dependency gate confirms Phase 4 and Plan 05-01 artifacts exist; otherwise execution stops with `BLOCKED_PLAN_DEPENDENCY`.
- [ ] `node scripts/validate-community-launch.mjs --section founder-review --section incident-feedback --section scanner-gate` passes.
- [ ] `npx vitest run tests/unit/revora/review-record.test.ts tests/unit/revora/scanner-gate.test.ts -x` passes.
- [ ] `rg -n "first 50|5 results per day|14 days|S0|S1|public_checks_enabled=false|evalBackfilled" docs/validation/founder-review-loop.md docs/ops/incident-feedback-loop.md` confirms review and incident workflows are connected.
- [ ] `rg -n "3 of 5|\\$5/month|10\\+ organic shares|not_earned" docs/validation/scanner-next-gate.md lib/revora/scanner-gate.ts` confirms the scanner-next gate is deterministic and aligned to `VALID-06`.
- [ ] `! rg -n "rawFood|rawA1c|promptText|fullModelOutput|username|contactInfo|privateDm|ip|userAgent" lib/revora/review-record.ts docs/validation/founder-review-loop.md docs/ops/incident-feedback-loop.md` confirms the founder review loop did not reintroduce raw production log storage.
</verification>

<success_criteria>
- Founder review covers the first 50 production results and at least 5 daily spot checks for 14 days.
- Review evidence is redacted and does not weaken the Phase 4 privacy boundary.
- S0/S1 incidents force eval backfill and tie to Phase 4 kill-switch or rollback evidence.
- Scanner-next is earned only by 3 of 5 `$5/month` WTP yeses or 10+ verified organic shares within two weeks.
- Scanner, native mobile, auth, saved history, payments, Stripe, and broader expansion stay deferred unless the deterministic gate is met.
</success_criteria>

<output>
After completion, create `.planning/phases/05-community-launch-and-founder-review-loop/05-02-SUMMARY.md`
</output>
