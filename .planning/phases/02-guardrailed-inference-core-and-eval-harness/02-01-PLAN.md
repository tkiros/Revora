---
phase: 02-guardrailed-inference-core-and-eval-harness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next-env.d.ts
  - next.config.ts
  - vitest.config.ts
  - lib/revora/schemas.ts
  - lib/revora/safety-contract.ts
  - lib/revora/openai-client.ts
  - lib/revora/prompt.ts
  - lib/revora/fallback.ts
  - lib/revora/service.ts
  - tests/unit/revora/schemas.test.ts
  - tests/unit/revora/service.test.ts
autonomous: true
requirements:
  - CLAIM-05
  - GUIDE-01
  - GUARD-01
  - GUARD-02
  - GUARD-03
user_setup:
  - service: openai
    why: "Live inference and launch-only live evals require a server-side OpenAI API key."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard API keys"
must_haves:
  truths:
    - "Every Revora check can run through one server-side `checkFood()` inference path."
    - "Malformed requests and malformed model outputs are schema validated before any response is rendered."
    - "Model contract failures retry once and then fail closed to safe retry copy."
    - "All user-facing responses include the Phase 1 doctor/RD informational-only disclaimer."
    - "OpenAI calls are server-side only and set `store: false`."
  artifacts:
    - path: "lib/revora/schemas.ts"
      provides: "Zod request, flat model-output, user-response schemas, and strict OpenAI JSON schema"
      exports: ["CheckRequestSchema", "RevoraModelOutputSchema", "revoraModelJsonSchema", "RevoraUserResponseSchema"]
    - path: "lib/revora/service.ts"
      provides: "Single `checkFood()` orchestrator used by API, tests, evals, and future UI"
      exports: ["checkFood"]
    - path: "lib/revora/openai-client.ts"
      provides: "Server-only OpenAI Responses API wrapper using strict structured outputs and `store: false`"
      contains: "store: false"
    - path: "lib/revora/fallback.ts"
      provides: "Safe retry, invalid-request, and controlled failure response copy"
      contains: "disclaimer"
    - path: "tests/unit/revora/service.test.ts"
      provides: "Single-path, malformed-output retry, and fail-closed service tests"
      contains: "malformed"
  key_links:
    - from: "lib/revora/service.ts"
      to: "lib/revora/openai-client.ts"
      via: "Injected model client dependency for production OpenAI and mocked tests"
      pattern: "model"
    - from: "lib/revora/openai-client.ts"
      to: "lib/revora/schemas.ts"
      via: "Responses API uses `revoraModelJsonSchema` with strict structured output"
      pattern: "revoraModelJsonSchema"
    - from: "lib/revora/service.ts"
      to: "lib/revora/fallback.ts"
      via: "Contract failures and invalid requests return controlled safe copy"
      pattern: "fallback"
    - from: "lib/revora/fallback.ts"
      to: "tests/fixtures/safety-contract.json"
      via: "Fallback copy includes the Phase 1 doctor/RD disclaimer"
      pattern: "disclaimer"
---

<objective>
Implement the structured-output check service and server-side guardrails for Revora.

Purpose: Phase 2 needs one controlled inference path before edge-case policy and launch evals can prove safety.
Output: Minimal TypeScript/Vitest scaffold if absent, typed Revora schemas, server-only OpenAI wrapper, `checkFood()` orchestrator, fail-closed fallback behavior, and core unit tests.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-RESEARCH.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VALIDATION.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-RESEARCH.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-01-PLAN.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-02-PLAN.md
@.planning/phases/01-claims-boundary-evidence-pack-and-safety-spec/01-03-PLAN.md

<interfaces>
Phase 1 is a hard prerequisite. At execution time, do not recreate or overwrite the safety contract in Phase 2. Before any Phase 2 implementation edits, run:

```bash
node scripts/validate-safety-contract.mjs
```

If it fails because `docs/safety/*`, `tests/fixtures/safety-contract.json`, or `scripts/validate-safety-contract.mjs` are missing, stop and report that Phase 1 has not been executed.

Create these Phase 2 contracts:

```typescript
export const CheckRequestSchema: z.ZodType<{
  food: string;
  a1c: number;
}>;

export type RevoraRisk = "SAFE" | "MODERATE" | "HIGH";
export type RevoraResponseKind =
  | "result"
  | "clarify"
  | "not_food"
  | "out_of_scope"
  | "retry";

export const revoraModelJsonSchema = {
  type: "object",
  additionalProperties: false,
  required: [
    "kind",
    "risk",
    "reason",
    "adjustment",
    "swap",
    "question",
    "examples",
    "policy_flags"
  ],
  properties: {
    kind: { type: "string", enum: ["result", "clarify", "not_food", "carbs_only"] },
    risk: { type: ["string", "null"], enum: ["SAFE", "MODERATE", "HIGH", null] },
    reason: { type: ["string", "null"] },
    adjustment: { type: ["string", "null"] },
    swap: { type: ["string", "null"] },
    question: { type: ["string", "null"] },
    examples: { type: "array", items: { type: "string" } },
    policy_flags: {
      type: "array",
      items: {
        type: "string",
        enum: ["safe_food", "borderline", "high_risk", "ambiguous", "carbs_only", "non_food"]
      }
    }
  }
} as const;

export async function checkFood(
  rawRequest: unknown,
  deps: { model: RevoraModelClient }
): Promise<RevoraUserResponse>;
```

The model-facing schema must stay a flat root object with `kind` and nullable required fields. Do not use a root discriminated union, root `anyOf`, `any`, LangChain, agents, RAG, vector databases, hosted eval uploads, scanner work, auth, database profiles, payments, or UI polish.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 - Verify Phase 1 and bootstrap typed test scaffold</name>
  <files>package.json, package-lock.json, tsconfig.json, next-env.d.ts, next.config.ts, vitest.config.ts, lib/revora/schemas.ts, tests/unit/revora/schemas.test.ts</files>
  <behavior>
    - Test 1: `CheckRequestSchema` accepts `{food: "lentil soup", a1c: 6.1}` and rejects missing, empty, oversized, nonnumeric, below-parser, or malformed values.
    - Test 2: `revoraModelJsonSchema` has root `type: "object"`, `additionalProperties: false`, all model fields listed in `required`, nullable field slots, and no root `anyOf`.
    - Test 3: `RevoraModelOutputSchema` rejects unknown risk values, missing required nullable fields, and extra model properties.
  </behavior>
  <action>First command before edits: run `node scripts/validate-safety-contract.mjs`. If it fails, stop and report the Phase 1 dependency failure; do not create replacement safety files in Phase 2. If no app/test scaffold exists, create the minimal Next.js/TypeScript/Vitest scaffold needed for server-side modules and unit tests: `package.json` with `next@16.2.4`, `react@19.2.5`, `react-dom@19.2.5`, `openai@6.36.0`, `zod@4.4.3`, dev dependencies `vitest@4.1.5`, `typescript@6.0.3`, `@types/node`, ESLint/Next config if needed, scripts `typecheck`, `test`, and `test:revora`; then run `npm install`. Keep scaffold minimal and do not create `app/layout.tsx`, `app/page.tsx`, or `app/api/check/route.ts` here; Plan 02-02 owns the app placeholders and thin API adapter. Write `vitest.config.ts` for Node-environment tests. Implement `lib/revora/schemas.ts` with strict Zod request/model/user-response schemas and the flat strict OpenAI JSON schema from the context. Write `tests/unit/revora/schemas.test.ts` before or alongside implementation so the expected contracts are explicit.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/schemas.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Phase 1 validator passes before implementation, minimal scaffold exists if it was absent, schema tests pass, and the model-facing schema is flat, strict, and structured-output compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add safety contract loader, prompt composer, and server-only OpenAI client</name>
  <files>lib/revora/safety-contract.ts, lib/revora/prompt.ts, lib/revora/openai-client.ts, tests/unit/revora/service.test.ts</files>
  <behavior>
    - Test 1: Safety contract loader reads `tests/fixtures/safety-contract.json` and exposes the approved disclaimer without hardcoding a conflicting Phase 2 copy source.
    - Test 2: Prompt composer includes the Phase 1 claims boundary, A1C scope reminder, qualitative-only rule, and strict output instructions without asking the model to invent diagnoses or exact glycemic numbers.
    - Test 3: OpenAI client calls the Responses API with `store: false`, `text.format.type: "json_schema"`, `strict: true`, and `revoraModelJsonSchema`.
  </behavior>
  <action>Implement `lib/revora/safety-contract.ts` as a read-only adapter around the Phase 1 fixture and safety docs. If expected keys are absent, throw a clear dependency error rather than inventing fallback policy. Implement `lib/revora/prompt.ts` as a compact developer/user prompt builder from the safety contract; it must keep Revora prediabetes-only, informational-only, qualitative, and conservative. Implement `lib/revora/openai-client.ts` as a server-only direct OpenAI SDK wrapper. It must create the SDK inside server code, require `OPENAI_API_KEY` for live calls, default `REVORA_MODEL` to `gpt-5.4-mini`, pass `store: false`, request strict structured output using `revoraModelJsonSchema`, parse `response.output_text`, and validate with `RevoraModelOutputSchema`. Add unit coverage with an injected fake OpenAI client so no network call is needed.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/schemas.test.ts tests/unit/revora/service.test.ts -t "OpenAI|prompt|safety contract"</automated>
  </verify>
  <done>Prompt and OpenAI client are server-only, use Phase 1 contract inputs, set `store: false`, and can be tested without network access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement single `checkFood()` path and fail-closed retries</name>
  <files>lib/revora/fallback.ts, lib/revora/service.ts, tests/unit/revora/service.test.ts</files>
  <behavior>
    - Test 1: `checkFood()` is the only exported service orchestrator and accepts an injected model dependency for production or tests.
    - Test 2: A malformed request returns a typed safe error or retry response without calling the model.
    - Test 3: A malformed model output retries once, then returns safe retry copy with the Phase 1 doctor/RD disclaimer.
    - Test 4: A valid in-scope model result returns `SAFE`, `MODERATE`, or `HIGH` only, with a one-sentence reason and disclaimer merged server-side.
  </behavior>
  <action>Implement `lib/revora/fallback.ts` for safe retry, invalid request, and generic controlled failure responses that include the Phase 1 disclaimer. Implement `lib/revora/service.ts` as the only core orchestrator: parse `CheckRequestSchema`, load the safety contract, call the injected model client at most twice for schema/contract failures, validate model output with `RevoraModelOutputSchema`, merge the Phase 1 disclaimer for user-facing output, and fail closed after retry exhaustion. Keep richer rubric invariants and the separate `lib/revora/postprocess.ts` module for Plan 02-02. Do not create `app/api/check/route.ts` in this plan; Plan 02-02 owns the thin API adapter. Tests must mock the model dependency, not duplicate the service path.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/schemas.test.ts tests/unit/revora/service.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`checkFood()` is the only core inference path, malformed inputs/outputs fail closed before rendering, and every core user-facing response includes the Phase 1 disclaimer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/validate-safety-contract.mjs` passes first and still passes at the end.
- [ ] `npx vitest run tests/unit/revora/schemas.test.ts tests/unit/revora/service.test.ts` passes.
- [ ] `npm run typecheck` passes.
- [ ] `rg -n "store: false" lib/revora/openai-client.ts` finds the OpenAI Responses call setting.
- [ ] `rg -n "from ['\\\"]openai['\\\"]" lib tests` shows OpenAI imported only from server-side wrapper/test fakes.
- [ ] No LangChain, agents, RAG, vector DB, hosted eval upload, scanner, auth, database profile, payment, or UI polish work was added.
</verification>

<success_criteria>
- Phase 2 execution is blocked until Phase 1 safety artifacts exist and `node scripts/validate-safety-contract.mjs` passes.
- One `checkFood()` service path owns request validation, model invocation, output validation, disclaimer merge, retry, and fallback behavior before adapter/rubric expansion in Plan 02-02.
- Model-facing structured output uses a flat strict root object with nullable required fields.
- OpenAI calls are server-side only and use `store: false`.
- Unit tests prove schema validation, single service path behavior, and malformed-output fail-closed behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-SUMMARY.md`
</output>
