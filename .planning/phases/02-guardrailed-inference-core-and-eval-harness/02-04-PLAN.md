---
phase: 02-guardrailed-inference-core-and-eval-harness
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - lib/revora/input-precheck.ts
  - tests/unit/revora/precheck.test.ts
  - tests/unit/revora/non-food-short-circuit.test.ts
  - tests/fixtures/revora-eval-cases.json
  - tests/support/revora-test-model.ts
autonomous: true
requirements:
  - INPUT-06
  - GUARD-01
  - GUARD-05
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "Ordinary object-like non-food inputs such as running shoes or laptop charger return `not_food` with concrete examples before any model call."
    - "Local non-food evals fail unless ordinary-object cases are satisfied by the deterministic refusal path instead of fixture-backed model output."
  artifacts:
    - path: "lib/revora/input-precheck.ts"
      provides: "High-confidence ordinary-object non-food detection before ambiguous and carbs-only routing"
      contains: "not_food"
    - path: "tests/unit/revora/non-food-short-circuit.test.ts"
      provides: "Service-level regression proving ordinary non-food inputs return `not_food` without invoking the model"
      contains: "not_food"
    - path: "tests/fixtures/revora-eval-cases.json"
      provides: "Ordinary non-food eval fixtures with no `mockModelOutput` fallback"
      contains: "\"category\": \"non_food\""
    - path: "tests/support/revora-test-model.ts"
      provides: "Eval guard that rejects mocked outputs for deterministic non-food short-circuit cases"
      contains: "mockModelOutput"
  key_links:
    - from: "lib/revora/service.ts"
      to: "lib/revora/input-precheck.ts"
      via: "`checkFood()` must classify ordinary-object non-food input before any `model.generate()` call"
      pattern: "classifyInputBeforeModel"
    - from: "tests/fixtures/revora-eval-cases.json"
      to: "tests/support/revora-test-model.ts"
      via: "Non-food fixtures omit `mockModelOutput`, so local evals fail if they ever reach the model seam"
      pattern: "mockModelOutput"
    - from: "tests/evals/revora-safety-eval.test.ts"
      to: "lib/revora/service.ts"
      via: "Existing non-food evals continue to assert final refusal behavior through `checkFood()`"
      pattern: "checkFood"
---

<objective>
Close the ordinary non-food refusal verification gap.

Purpose: Phase 2 is not complete until obvious object-like non-food inputs are deterministically refused in production code rather than being satisfied by mocked eval output.
Output: Narrow ordinary-object non-food detection, service-level no-model-call regression coverage, and eval fixtures that can only pass when the deterministic refusal fires before the model seam.
</objective>

<execution_context>
@/home/tefera/.claude/get-shit-done/workflows/execute-plan.md
@/home/tefera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VERIFICATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VALIDATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-SUMMARY.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-03-SUMMARY.md
@lib/revora/input-precheck.ts
@lib/revora/service.ts
@tests/fixtures/revora-eval-cases.json
@tests/support/revora-test-model.ts

<interfaces>
Current pre-model seam:

```typescript
export type InputPrecheck =
  | { kind: "ok"; flags: RevoraPolicyFlag[] }
  | { kind: "not_food"; examples: string[] }
  | { kind: "clarify"; question: string }
  | { kind: "carbs_only"; flags: RevoraPolicyFlag[] };

export function classifyInputBeforeModel(food: string): InputPrecheck;
```

Current limitation called out by verification:

```typescript
function looksLikeNonFood(food: string): boolean {
  return PROMPT_INJECTION_PATTERNS.some((pattern) => pattern.test(food));
}
```

Existing eval seam to reuse instead of adding a second classifier path:

```typescript
export function createEvalModelClient(
  cases: readonly RevoraEvalCase[]
): RevoraModelClient;

if (!evalCase.mockModelOutput) {
  throw new Error(
    `Eval fixture ${evalCase.id} reached the model path without mockModelOutput.`
  );
}
```

Use that failure path as the proof: ordinary non-food fixtures should stop needing `mockModelOutput` because `checkFood()` must short-circuit them before the model call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand ordinary-object non-food precheck and prove model bypass</name>
  <files>lib/revora/input-precheck.ts, tests/unit/revora/precheck.test.ts, tests/unit/revora/non-food-short-circuit.test.ts</files>
  <behavior>
    - Test 1: `classifyInputBeforeModel("running shoes")`, `("laptop charger")`, `("dish soap")`, `("water bottle")`, and `("phone case")` all return `kind: "not_food"` with concrete examples.
    - Test 2: `checkFood()` returns `kind: "not_food"` for those ordinary-object inputs and never calls `model.generate()`.
    - Test 3: A real food such as `eggs with spinach` or `lentil soup with salad` does not fall into the ordinary-object non-food path.
  </behavior>
  <action>Extend `looksLikeNonFood()` or an equivalent helper with a narrow, high-confidence ordinary-object lexicon that covers the verification fixtures and similarly obvious household/accessory objects. Preserve the existing prompt-injection detection. Run this check before ambiguity and carbs-only routing. Do NOT broaden this into a generic noun blacklist or start classifying plausible foods, drinks, or packaging phrases as non-food. Update `tests/unit/revora/precheck.test.ts` with direct object-input coverage, and add `tests/unit/revora/non-food-short-circuit.test.ts` to call `checkFood()` with a mocked model and assert `kind: "not_food"` plus zero model calls.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/precheck.test.ts tests/unit/revora/non-food-short-circuit.test.ts</automated>
  </verify>
  <done>Ordinary object-like non-food inputs are deterministically refused before the model seam, and service-level tests prove no model invocation occurs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove non-food eval model crutches and lock the short-circuit gate</name>
  <files>tests/fixtures/revora-eval-cases.json, tests/support/revora-test-model.ts</files>
  <behavior>
    - Test 1: The five `non_food` eval fixtures no longer declare `mockModelOutput`.
    - Test 2: The local fixture loader/client rejects deterministic short-circuit categories that still try to ship `mockModelOutput`.
    - Test 3: `tests/evals/revora-safety-eval.test.ts -t "non-food"` passes, which now implies the service short-circuited before model access because the local model seam would otherwise throw.
  </behavior>
  <action>Delete `mockModelOutput` from the ordinary-object `non_food` fixtures (`running shoes`, `laptop charger`, `dish soap`, `water bottle`, `phone case`). In `tests/support/revora-test-model.ts`, add explicit validation that local deterministic short-circuit categories expected to resolve before the model — at minimum `non_food`, and only include other categories if they already short-circuit today without widening scope — may not define `mockModelOutput`. Keep live mode unchanged. Do not add a second classifier seam inside the eval harness. Why: `INPUT-06` is only satisfied if refusal happens in production code, not if eval fixtures fabricate `not_food` outputs.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const cases=JSON.parse(fs.readFileSync('tests/fixtures/revora-eval-cases.json','utf8')); const bad=cases.filter((item)=>item.category==='non_food' && item.mockModelOutput); if (bad.length) throw new Error(bad.map((item)=>item.id).join(','));" && npx vitest run tests/evals/revora-safety-eval.test.ts -t "non-food"</automated>
  </verify>
  <done>Local non-food evals can no longer hide model calls behind fixture output, so passing evals now prove deterministic refusal for ordinary object inputs.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/precheck.test.ts tests/unit/revora/non-food-short-circuit.test.ts` passes.
- [ ] `npx vitest run tests/evals/revora-safety-eval.test.ts -t "non-food"` passes.
- [ ] `node -e "const fs=require('node:fs'); const cases=JSON.parse(fs.readFileSync('tests/fixtures/revora-eval-cases.json','utf8')); const bad=cases.filter((item)=>item.category==='non_food' && item.mockModelOutput); if (bad.length) throw new Error(bad.map((item)=>item.id).join(','));"` finds zero mocked non-food fixtures.
- [ ] No new dependency, classifier seam, hosted eval upload path, or broad food parser was added.
</verification>

<success_criteria>
- Ordinary-object inputs in the verification gap now resolve to `not_food` through `checkFood()` without touching the model.
- The local eval corpus no longer provides mocked `not_food` outputs for ordinary non-food object fixtures.
- A future regression that sends those fixtures to the model fails the targeted non-food eval immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-04-SUMMARY.md`
</output>
