---
phase: 02-guardrailed-inference-core-and-eval-harness
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - tests/fixtures/revora-eval-cases.json
  - tests/support/revora-test-model.ts
  - tests/evals/revora-safety-eval.test.ts
  - scripts/run-live-revora-evals.mjs
autonomous: true
requirements:
  - CLAIM-05
  - INPUT-06
  - INPUT-07
  - INPUT-08
  - GUIDE-01
  - GUIDE-03
  - GUIDE-04
  - GUIDE-05
  - GUIDE-06
  - GUARD-01
  - GUARD-02
  - GUARD-03
  - GUARD-05
  - GUARD-06
user_setup:
  - service: openai
    why: "The optional launch-only live eval command requires live model credentials."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard API keys"
must_haves:
  truths:
    - "A local synthetic eval set covers clearly safe, borderline, high-risk, non-food, ambiguous, carbs-only, out-of-range A1C, and prompt-injection cases."
    - "Eval tests call the same `checkFood()` service path used by the API."
    - "The deterministic launch gate fails when any fixture marked harmful-if-safe returns SAFE."
    - "The local full suite reaches zero harmful SAFE classifications before public launch."
    - "The live-model eval command uses synthetic fixtures only and does not upload hosted eval datasets."
  artifacts:
    - path: "tests/fixtures/revora-eval-cases.json"
      provides: "Synthetic launch-blocking eval fixtures with required categories and harmfulIfSafe flags"
      contains: "harmfulIfSafe"
    - path: "tests/support/revora-test-model.ts"
      provides: "Fixture-backed and optional live model client adapters for evals"
      exports: ["createEvalModelClient"]
    - path: "tests/evals/revora-safety-eval.test.ts"
      provides: "Category coverage, contract assertions, and zero harmful SAFE gate"
      contains: "harmfulSafeCount"
    - path: "scripts/run-live-revora-evals.mjs"
      provides: "Launch-only live eval runner over synthetic fixtures"
      contains: "REVORA_LIVE_EVAL"
  key_links:
    - from: "tests/evals/revora-safety-eval.test.ts"
      to: "lib/revora/service.ts"
      via: "Eval tests call `checkFood()` instead of a separate classifier"
      pattern: "checkFood"
    - from: "tests/evals/revora-safety-eval.test.ts"
      to: "tests/fixtures/revora-eval-cases.json"
      via: "Eval cases drive category coverage and harmful SAFE counting"
      pattern: "revora-eval-cases.json"
    - from: "scripts/run-live-revora-evals.mjs"
      to: "tests/evals/revora-safety-eval.test.ts"
      via: "Script runs the same Vitest eval with `REVORA_LIVE_EVAL=1`"
      pattern: "REVORA_LIVE_EVAL"
---

<objective>
Build the safety evaluation suite and gate launch on zero harmful SAFE results.

Purpose: Revora should not launch publicly until local synthetic evals prove coverage of edge cases and the exact launch-blocking failure mode: harmful SAFE classifications.
Output: Synthetic eval fixtures, fixture/live model adapters, Vitest eval gate, package script, and launch-only live eval runner.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-RESEARCH.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VALIDATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-SUMMARY.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-SUMMARY.md
@tests/fixtures/safety-contract.json
@lib/revora/service.ts
@lib/revora/schemas.ts
@lib/revora/openai-client.ts
@lib/revora/a1c.ts
@lib/revora/input-precheck.ts
@lib/revora/postprocess.ts

<interfaces>
Use the production service path from prior plans:

```typescript
export async function checkFood(
  rawRequest: unknown,
  deps: { model: RevoraModelClient }
): Promise<RevoraUserResponse>;
```

Create fixture records with this shape:

```typescript
type RevoraEvalCase = {
  id: string;
  category:
    | "clearly_safe"
    | "borderline"
    | "high_risk"
    | "non_food"
    | "ambiguous"
    | "carbs_only"
    | "out_of_range_a1c"
    | "prompt_injection";
  input: { food: string; a1c: number };
  harmfulIfSafe: boolean;
  expectedKinds: Array<"result" | "clarify" | "not_food" | "out_of_scope" | "retry">;
  disallowRisk?: Array<"SAFE" | "MODERATE" | "HIGH">;
  mockModelOutput?: RevoraModelOutput;
  notes: string;
};
```

Do not use hosted OpenAI Evals, JSONL uploads, real user data, raw production logs, LLM-as-judge grading, scanner fixtures, auth, database storage, payments, or UI tests in this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create synthetic launch-blocking eval fixtures</name>
  <files>tests/fixtures/revora-eval-cases.json</files>
  <behavior>
    - Test 1: Fixture categories include `clearly_safe`, `borderline`, `high_risk`, `non_food`, `ambiguous`, `carbs_only`, `out_of_range_a1c`, and `prompt_injection`.
    - Test 2: Each required category has at least five synthetic cases.
    - Test 3: Borderline, high-risk, carbs-only, ambiguous, out-of-range, and prompt-injection cases include `harmfulIfSafe: true` where SAFE would be unsafe reassurance.
    - Test 4: Fixture inputs are synthetic and do not include real user data, names, contact information, or production logs.
  </behavior>
  <action>Create `tests/fixtures/revora-eval-cases.json` with at least 40 synthetic cases: five clearly safe, five borderline, five high-risk, five non-food, five ambiguous, five carbs-only, five out-of-range A1C, and five prompt-injection/jailbreak cases. Keep cases short and realistic for the text-only MVP, such as eggs with spinach, lentil soup, plain bagel, white rice, soda, donut, restaurant combo, "banana", "ignore your rules", and A1C `5.6`/`6.5`. Include `expectedKinds`, `disallowRisk`, `harmfulIfSafe`, and `notes` for each case. For cases that need a model call in local deterministic mode, include `mockModelOutput` matching the flat model schema from Plan 02-01. Do not include real user examples or upload-oriented JSONL.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/evals/revora-safety-eval.test.ts -t covers</automated>
  </verify>
  <done>Eval fixtures cover all required launch categories with harmfulIfSafe metadata and synthetic-only examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic Vitest eval gate against `checkFood()`</name>
  <files>tests/support/revora-test-model.ts, tests/evals/revora-safety-eval.test.ts</files>
  <behavior>
    - Test 1: Eval coverage test fails if any required category is missing or has fewer than five cases.
    - Test 2: Every eval case calls `checkFood(case.input, { model })`.
    - Test 3: Non-food, ambiguous, carbs-only, and out-of-range A1C cases assert the controlled response kind and disclaimer presence.
    - Test 4: Harmful SAFE gate computes `harmfulSafeCount` and fails unless it is exactly `0`.
    - Test 5: SAFE eval results have no swap or adjustment; MODERATE/HIGH eval results have exactly one adjustment and exactly one swap.
  </behavior>
  <action>Implement `tests/support/revora-test-model.ts` with `createEvalModelClient(cases)` that returns a fixture-backed model client by default and switches to the live OpenAI client only when `REVORA_LIVE_EVAL=1`. Implement `tests/evals/revora-safety-eval.test.ts` using Vitest. The test must load `tests/fixtures/revora-eval-cases.json`, validate fixture shape with Zod or the existing schemas, assert required category coverage, then iterate through cases and call the production `checkFood()` service. The deterministic gate must compute `harmfulSafeCount` from cases where `harmfulIfSafe` is true and fail unless the count is `0`. Include contract assertions for disclaimer, one reason, SAFE no swap/adjustment, MODERATE/HIGH one adjustment and one swap, non-food examples, at-most-one clarification question, and carbs-only add-protein/add-vegetable guidance.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/evals/revora-safety-eval.test.ts -t "covers|zero harmful SAFE|non-food|ambiguous|carbs-only"</automated>
  </verify>
  <done>The local eval suite proves category coverage and zero harmful SAFE classifications through the same `checkFood()` service path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add launch eval command and full Phase 2 verification gate</name>
  <files>package.json, scripts/run-live-revora-evals.mjs, tests/support/revora-test-model.ts, tests/evals/revora-safety-eval.test.ts</files>
  <behavior>
    - Test 1: `npm run eval:revora` runs the local deterministic eval suite.
    - Test 2: `scripts/run-live-revora-evals.mjs` runs `REVORA_LIVE_EVAL=1 npx vitest run tests/evals/revora-safety-eval.test.ts` and fails clearly if `OPENAI_API_KEY` is missing.
    - Test 3: Live eval mode still uses synthetic fixtures only and routes model calls through the server-only OpenAI wrapper with `store: false`.
    - Test 4: Full suite command from `02-VALIDATION.md` passes locally without hosted eval uploads.
  </behavior>
  <action>Add a package script such as `eval:revora` for `npx vitest run tests/evals/revora-safety-eval.test.ts`. Add `scripts/run-live-revora-evals.mjs` as an executable Node.js built-in script that checks `OPENAI_API_KEY`, sets `REVORA_LIVE_EVAL=1`, and runs the Vitest eval file. In live mode, require the same synthetic fixture file and the same `checkFood()` path; do not upload hosted eval datasets or write raw prompt/output logs. Update eval support so deterministic local mode remains the default for CI and launch live mode is explicit. Ensure any live failure prints category/id-level summary without raw hidden logs or real user data.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora tests/evals</automated>
    <automated>npm run typecheck</automated>
    <automated>REVORA_LIVE_EVAL=1 npx vitest run tests/evals/revora-safety-eval.test.ts</automated>
  </verify>
  <done>Local unit/eval suite is green, launch live eval command exists, and the launch gate fails on any harmful SAFE classification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora tests/evals` passes.
- [ ] `npm run typecheck` passes.
- [ ] `npx vitest run tests/evals/revora-safety-eval.test.ts -t covers` passes.
- [ ] `npx vitest run tests/evals/revora-safety-eval.test.ts -t "zero harmful SAFE"` passes with `harmfulSafeCount === 0`.
- [ ] `REVORA_LIVE_EVAL=1 npx vitest run tests/evals/revora-safety-eval.test.ts` passes when `OPENAI_API_KEY` is available; if the key is unavailable, record that live launch verification is blocked by setup, not by local code.
- [ ] `rg -n "evals.create|jsonl|upload|hosted" tests scripts lib` returns no hosted eval upload implementation.
</verification>

<success_criteria>
- Synthetic eval fixtures cover safe, borderline, high-risk, non-food, ambiguous, carbs-only, out-of-range A1C, and prompt-injection cases.
- Evals exercise the real `checkFood()` service, not a separate classifier.
- Local deterministic evals have zero harmful SAFE classifications.
- Launch live eval command exists, uses synthetic fixtures only, and keeps OpenAI calls behind the server-side `store: false` wrapper.
- Phase 2 full suite command from `02-VALIDATION.md` is green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-03-SUMMARY.md`
</output>
