---
phase: 02-guardrailed-inference-core-and-eval-harness
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/api/check/route.ts
  - lib/revora/a1c.ts
  - lib/revora/input-precheck.ts
  - lib/revora/fallback.ts
  - lib/revora/postprocess.ts
  - lib/revora/prompt.ts
  - lib/revora/service.ts
  - tests/unit/revora/a1c.test.ts
  - tests/unit/revora/precheck.test.ts
  - tests/unit/revora/postprocess.test.ts
  - tests/unit/revora/service.test.ts
autonomous: true
requirements:
  - CLAIM-05
  - INPUT-06
  - INPUT-07
  - INPUT-08
  - GUIDE-01
  - GUIDE-03
  - GUIDE-04
  - GUIDE-05
  - GUIDE-06
  - GUARD-01
  - GUARD-02
  - GUARD-03
user_setup: []
must_haves:
  truths:
    - "Out-of-range A1C values return controlled out-of-scope guidance and never receive SAFE, MODERATE, or HIGH."
    - "Non-food inputs refuse classification and show concrete food examples."
    - "Ambiguous food descriptions ask at most one clarifying question or route conservatively without invented details."
    - "Carbs-only meals recommend adding protein or nonstarchy vegetables instead of impossible sequencing-only advice."
    - "SAFE results are permission-first with no adjustment and no swap; MODERATE and HIGH include exactly one adjustment and exactly one swap."
    - "`POST /api/check` delegates to `checkFood()` as a thin server adapter after the service policy is wired."
  artifacts:
    - path: "app/api/check/route.ts"
      provides: "Thin Node.js POST adapter for the single service path"
      exports: ["POST"]
    - path: "lib/revora/a1c.ts"
      provides: "Deterministic A1C band routing from Phase 1 safety contract"
      exports: ["routeA1C"]
    - path: "lib/revora/input-precheck.ts"
      provides: "Deterministic non-food, ambiguous, carbs-only, and length/scope prechecks"
      exports: ["classifyInputBeforeModel"]
    - path: "lib/revora/postprocess.ts"
      provides: "Risk invariant enforcement, conservative floors, one-sentence checks, disclaimer merge"
      contains: "applyConservativeFloors"
    - path: "tests/unit/revora/precheck.test.ts"
      provides: "Edge-case tests for non-food, ambiguous, and carbs-only inputs"
      contains: "carbs-only"
    - path: "tests/unit/revora/postprocess.test.ts"
      provides: "SAFE/MODERATE/HIGH shape and disclaimer contract tests"
      contains: "exactly one"
  key_links:
    - from: "app/api/check/route.ts"
      to: "lib/revora/service.ts"
      via: "POST handler calls `checkFood()` and does not import OpenAI or prompt code directly"
      pattern: "checkFood"
    - from: "lib/revora/service.ts"
      to: "lib/revora/a1c.ts"
      via: "`checkFood()` routes A1C before any model call"
      pattern: "routeA1C"
    - from: "lib/revora/service.ts"
      to: "lib/revora/input-precheck.ts"
      via: "`checkFood()` handles deterministic edge cases before prompt/model invocation"
      pattern: "classifyInputBeforeModel"
    - from: "lib/revora/postprocess.ts"
      to: "tests/fixtures/safety-contract.json"
      via: "Conservative floors and disclaimer are derived from Phase 1 fixture/policy"
      pattern: "safetyContract"
---

<objective>
Implement edge-case handling, A1C rubric application, and conservative fallback behavior.

Purpose: The service created in Plan 02-01 must honor the Phase 1 safety contract instead of relying on prompt obedience for non-food, ambiguous, carbs-only, out-of-scope, SAFE, MODERATE, and HIGH behavior.
Output: Deterministic A1C/input prechecks, conservative post-processing floors, prompt updates, the thin API adapter/minimal app placeholders moved out of Plan 02-01, and unit tests that prove controlled behavior for edge cases and in-scope results.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-RESEARCH.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VALIDATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-SUMMARY.md
@docs/safety/claims-boundary.md
@docs/safety/a1c-band-rubric.md
@docs/safety/tone-uncertainty-policy.md
@tests/fixtures/safety-contract.json
@lib/revora/schemas.ts
@lib/revora/service.ts
@lib/revora/postprocess.ts
@lib/revora/fallback.ts
@lib/revora/prompt.ts

<interfaces>
Plan 02-01 creates these contracts. Preserve names and import paths unless its SUMMARY says execution changed them:

```typescript
export async function checkFood(
  rawRequest: unknown,
  deps: { model: RevoraModelClient }
): Promise<RevoraUserResponse>;

export type RevoraRisk = "SAFE" | "MODERATE" | "HIGH";
export type RevoraResponseKind = "result" | "clarify" | "not_food" | "out_of_scope" | "retry";
```

Add these deterministic helpers:

```typescript
export type A1CBand =
  | "below_prediabetes_range"
  | "prediabetes_57_59"
  | "prediabetes_60_62"
  | "prediabetes_63_64"
  | "diabetes_range_out_of_scope";

export function routeA1C(a1c: number): A1CRoute;

export type InputPrecheck =
  | { kind: "ok"; flags: string[] }
  | { kind: "not_food"; examples: string[] }
  | { kind: "clarify"; question: string }
  | { kind: "carbs_only"; flags: string[] };

export function classifyInputBeforeModel(food: string): InputPrecheck;
```

Do not add broad food parsing, exact GI/GL scoring, future-A1C prediction, medical advice, hosted evals, database storage, auth, scanner logic, or UI polish.

This plan owns the minimal Next app placeholders and `app/api/check/route.ts` because Plan 02-01 is intentionally limited to scaffold/config plus the core service foundation. Keep `app/layout.tsx` and `app/page.tsx` compile-only placeholders; do not implement the public mobile UI until Phase 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic A1C routing and input prechecks</name>
  <files>lib/revora/a1c.ts, lib/revora/input-precheck.ts, lib/revora/fallback.ts, lib/revora/service.ts, tests/unit/revora/a1c.test.ts, tests/unit/revora/precheck.test.ts, tests/unit/revora/service.test.ts</files>
  <behavior>
    - Test 1: A1C below `5.7` returns `out_of_scope_below`; `5.7-5.9`, `6.0-6.2`, and `6.3-6.4` return in-scope band ids; `6.5+` returns `out_of_scope_high`.
    - Test 2: Below-range and high-range A1C inputs do not call the model and do not return SAFE, MODERATE, or HIGH.
    - Test 3: Obvious non-food input returns `not_food` with at least three concrete food examples and no risk classification.
    - Test 4: Too-vague or ambiguous foods return one clarifying question, not multiple questions and not invented assumptions.
    - Test 5: Carbs-only inputs are flagged before the model so post-processing can enforce add-protein/add-vegetable guidance.
  </behavior>
  <action>Implement `routeA1C()` directly from the executed Phase 1 `docs/safety/a1c-band-rubric.md` and `tests/fixtures/safety-contract.json`; these are scope routes, not diagnoses. Implement high-confidence `classifyInputBeforeModel()` checks only: empty/oversized text, obvious non-food or prompt-injection-like text, too-vague ambiguous food descriptions, and carbs-only meals such as plain bagel, white rice, pasta, tortilla, cereal, candy, pastry, or sugary drink with no protein/nonstarchy vegetable context. Keep this intentionally narrow; do not build a broad parser. Update `fallback.ts` with deterministic out-of-scope, not-food, clarify, and carbs-only-safe responses that include the Phase 1 disclaimer. Wire `service.ts` so A1C and deterministic prechecks run before model invocation and test that the mocked model is not called for deterministic short-circuits.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/a1c.test.ts tests/unit/revora/precheck.test.ts tests/unit/revora/service.test.ts -t "A1C|non-food|ambiguous|carbs-only|out-of-scope"</automated>
  </verify>
  <done>A1C scope and deterministic edge cases are handled before model calls, without conflicting with the Phase 1 safety contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce result rubric, one-sentence fields, and conservative floors</name>
  <files>lib/revora/postprocess.ts, lib/revora/service.ts, tests/unit/revora/postprocess.test.ts, tests/unit/revora/service.test.ts</files>
  <behavior>
    - Test 1: In-scope `result` responses only expose `SAFE`, `MODERATE`, or `HIGH`.
    - Test 2: Every in-scope result has exactly one plain-English reason sentence.
    - Test 3: SAFE results are permission-first and reject non-null adjustment or swap.
    - Test 4: MODERATE and HIGH results require exactly one practical adjustment and exactly one lower-glycemic swap.
    - Test 5: Upper-band A1C plus uncertain carb-containing food cannot remain SAFE after post-processing.
    - Test 6: Contract violations retry once through `checkFood()` and then fail closed to safe retry copy.
  </behavior>
  <action>Extend `postprocess.ts` with named invariant checks: `assertOneSentence`, `assertNoUnsafeSafeFields`, `assertModerateHighFields`, and `applyConservativeFloors`. SAFE must lead with permission-first reassurance, have a reason, and have `adjustment: null` and `swap: null` in user-facing output. MODERATE and HIGH must each have one reason sentence, exactly one practical adjustment, and exactly one lower-glycemic swap. For carbs-only cases, accepted adjustments must add protein, fiber, or nonstarchy vegetables; sequencing-only advice is invalid. Use Phase 1 tone/uncertainty policy for conservative floors, especially ambiguous carb-containing foods and A1C `6.3-6.4`. When model output violates these invariants, throw a typed contract error so `checkFood()` performs the bounded retry from Plan 02-01 and then returns safe retry copy if needed.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/postprocess.test.ts tests/unit/revora/service.test.ts -t "safe|reason|adjustment|swap|conservative|malformed"</automated>
  </verify>
  <done>In-scope outputs meet the SAFE/MODERATE/HIGH rubric exactly, and unsafe reassurance is converted into retry/fallback behavior rather than rendered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align prompt instructions, service integration, and API adapter with edge-case policy</name>
  <files>app/layout.tsx, app/page.tsx, app/api/check/route.ts, lib/revora/prompt.ts, lib/revora/service.ts, tests/unit/revora/service.test.ts</files>
  <behavior>
    - Test 1: Prompt text includes the allowed response kinds and tells the model not to classify out-of-scope A1C, non-food, or ambiguous inputs with invented details.
    - Test 2: Service sends precheck flags and A1C band context into the prompt only after deterministic scope checks pass.
    - Test 3: Valid SAFE, MODERATE, HIGH, clarify, not-food, carbs-only, retry, and out-of-scope cases all include the Phase 1 disclaimer.
    - Test 4: `POST /api/check` delegates to `checkFood()` and does not import the OpenAI SDK or prompt builder directly.
  </behavior>
  <action>Update `prompt.ts` so the model gets compact Phase 1 policy: prediabetes-only scope, qualitative guidance only, no diagnosis/treatment/reversal/future-A1C/exact-number claims, SAFE no swap, MODERATE/HIGH exactly one adjustment and one swap, one clarifying question at most, and carbs-only add-protein/add-vegetable guidance. Update `service.ts` to pass A1C band, conservative level, and input precheck flags into the prompt when a model call is appropriate. Create minimal `app/layout.tsx` and `app/page.tsx` placeholders if the scaffold still needs them for Next compilation; keep them compile-only with no public UI feature work. Implement `app/api/check/route.ts` as a thin Node.js `POST` handler that calls `checkFood()` and returns typed JSON; do not add route-level prompt logic, OpenAI imports, UI logic, auth, telemetry, or storage.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/a1c.test.ts tests/unit/revora/precheck.test.ts tests/unit/revora/postprocess.test.ts tests/unit/revora/service.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>rg -n "checkFood" app/api/check/route.ts && ! rg -n "from ['\\\"]openai|prompt" app/api/check/route.ts</automated>
  </verify>
  <done>The service integrates deterministic policy, prompt context, and post-processing invariants through the single `checkFood()` path; the API route is a thin adapter; the full Phase 2 unit suite passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/validate-safety-contract.mjs` passes.
- [ ] `npx vitest run tests/unit/revora/a1c.test.ts tests/unit/revora/precheck.test.ts tests/unit/revora/postprocess.test.ts tests/unit/revora/service.test.ts` passes.
- [ ] `npm run typecheck` passes.
- [ ] `rg -n "routeA1C|classifyInputBeforeModel" lib/revora/service.ts` confirms deterministic gates are in the single service path.
- [ ] `rg -n "checkFood" app/api/check/route.ts && ! rg -n "from ['\\\"]openai|prompt" app/api/check/route.ts` confirms the API route delegates without direct OpenAI or prompt logic.
- [ ] No exact GI, GL, mg/dL spike, future-A1C, diagnosis, treatment, reversal, scanner, auth, database, payment, hosted eval, or UI polish work was added.
</verification>

<success_criteria>
- Non-food, ambiguous, carbs-only, below-range A1C, and `6.5+` A1C cases resolve through controlled behavior instead of invented details.
- In-scope foods return only SAFE, MODERATE, or HIGH.
- SAFE results are permission-first and omit unnecessary swaps and adjustments.
- MODERATE and HIGH results include exactly one practical adjustment and exactly one lower-glycemic swap.
- Conservative floors prevent harmful SAFE classifications for uncertain or borderline cases.
- `POST /api/check` exists as a thin adapter over `checkFood()` after edge-case policy is wired.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-SUMMARY.md`
</output>
