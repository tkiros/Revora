---
phase: 02-guardrailed-inference-core-and-eval-harness
plan: "05"
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - lib/revora/postprocess.ts
  - tests/unit/revora/postprocess.test.ts
  - tests/unit/revora/carbs-only-floor.test.ts
  - tests/evals/revora-safety-eval.test.ts
autonomous: true
requirements:
  - INPUT-08
  - GUIDE-05
  - GUIDE-06
  - GUARD-05
  - GUARD-06
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "Carbs-only meals never return sequencing-only guidance; final adjustment text explicitly adds or pairs the meal with protein or nonstarchy vegetables, or the response is floored to deterministic fallback copy."
    - "Local regression tests and carbs-only eval assertions fail if final guidance says only to eat vegetables first, start with vegetables, or similar sequencing-only phrasing."
  artifacts:
    - path: "lib/revora/postprocess.ts"
      provides: "Stricter carbs-only adjustment validator and deterministic flooring for sequencing-only guidance"
      contains: "carbs_only"
    - path: "tests/unit/revora/postprocess.test.ts"
      provides: "Unit regressions rejecting sequencing-only carbs-only adjustments"
      contains: "vegetables first"
    - path: "tests/unit/revora/carbs-only-floor.test.ts"
      provides: "Service-level regression proving `checkFood()` floors sequencing-only model output to deterministic carbs-only copy"
      contains: "checkFood"
    - path: "tests/evals/revora-safety-eval.test.ts"
      provides: "Carbs-only eval assertions that require add/pair-with guidance and reject sequencing-only-only copy"
      contains: "carbs_only"
  key_links:
    - from: "lib/revora/postprocess.ts"
      to: "lib/revora/fallback.ts"
      via: "Invalid carbs-only adjustments floor to `buildCarbsOnlyResponse()` instead of rendering model prose"
      pattern: "buildCarbsOnlyResponse"
    - from: "tests/unit/revora/carbs-only-floor.test.ts"
      to: "lib/revora/service.ts"
      via: "Injected sequencing-only model output must still return deterministic add-protein/add-vegetable copy"
      pattern: "checkFood"
    - from: "tests/evals/revora-safety-eval.test.ts"
      to: "lib/revora/service.ts"
      via: "Carbs-only evals assert final user-facing output through `checkFood()`, not raw model text"
      pattern: "checkFood"
---

<objective>
Close the carbs-only guidance enforcement verification gap.

Purpose: Phase 2 is not complete until carbs-only meals are prevented from shipping impossible sequencing-only advice and are deterministically floored to add-protein/add-vegetable guidance when model wording drifts.
Output: Stronger carbs-only postprocessing semantics, service-level flooring regression coverage, and eval assertions that reject sequencing-only final guidance.
</objective>

<execution_context>
@/home/tefera/.claude/get-shit-done/workflows/execute-plan.md
@/home/tefera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VERIFICATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-VALIDATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-SUMMARY.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-03-SUMMARY.md
@lib/revora/postprocess.ts
@lib/revora/fallback.ts
@lib/revora/service.ts
@tests/evals/revora-safety-eval.test.ts

<interfaces>
Current carbs-only enforcement seam:

```typescript
export function assertModerateHighFields(
  result: ResultDraft,
  flags: ReadonlySet<RevoraPolicyFlag>
): void;

export function applyConservativeFloors(
  result: ResultDraft,
  context: PostprocessContext
): ResultDraft;
```

Current limitation called out by verification:

```typescript
if (flags.has("carbs_only") && !mentionsProteinOrVegetables(result.adjustment)) {
  throw new RevoraContractError(
    "Carbs-only adjustments must add protein or nonstarchy vegetables."
  );
}

function mentionsProteinOrVegetables(value: string | null): boolean {
  return /\bprotein\b|\bnonstarchy vegetables\b|\bvegetables\b|\bfiber\b/i.test(
    value ?? ""
  );
}
```

Existing deterministic floor copy already satisfies INPUT-08 and should be reused rather than replaced:

```typescript
export function buildCarbsOnlyResponse(
  contract: SafetyContract,
  risk: "MODERATE" | "HIGH" = "MODERATE"
): RevoraUserResponse;
```

Current eval weakness to tighten:

```typescript
/protein|nonstarchy vegetables|vegetables/i.test(
  run.response.adjustment ?? ""
)
```

Strengthen semantics beyond keyword presence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace keyword-only carbs-only validation with add/pair-with semantics</name>
  <files>lib/revora/postprocess.ts, tests/unit/revora/postprocess.test.ts</files>
  <behavior>
    - Test 1: Adjustments like `Eat vegetables first if you can.` or `Start with vegetables before the carbs.` fail carbs-only validation even though they mention vegetables.
    - Test 2: Adjustments like `Add protein or nonstarchy vegetables to this meal.` or `Pair it with eggs or a side salad.` pass carbs-only validation.
    - Test 3: SAFE or otherwise invalid carbs-only outputs are floored back to the deterministic `buildCarbsOnlyResponse()` copy.
  </behavior>
  <action>Replace `mentionsProteinOrVegetables()` or an equivalent helper with a stricter carbs-only detector that requires add/pair-with/include/alongside-style guidance tied to protein or nonstarchy vegetables. Explicitly treat sequencing-first/before/start-with language as invalid when it lacks add/pair semantics. Keep non-carbs-only MODERATE/HIGH validation unchanged. Reuse the existing deterministic fallback copy from `buildCarbsOnlyResponse()` instead of inventing new prose. Update `tests/unit/revora/postprocess.test.ts` with both rejection and acceptance cases so INPUT-08 is enforced semantically, not by keywords alone.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/postprocess.test.ts -t "carbs-only|sequencing|adjustment"</automated>
  </verify>
  <done>Carbs-only postprocessing no longer accepts sequencing-only wording and falls back to deterministic add-protein/add-vegetable guidance when the model drifts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add service regression and tighten carbs-only eval assertions</name>
  <files>tests/unit/revora/carbs-only-floor.test.ts, tests/evals/revora-safety-eval.test.ts</files>
  <behavior>
    - Test 1: `checkFood()` floors a model output whose carbs-only adjustment says only `Eat vegetables first if you can.` to the deterministic fallback copy.
    - Test 2: Carbs-only eval assertions require final adjustments to contain add/pair-with semantics and reject sequencing-only-only phrasing.
    - Test 3: Final carbs-only eval results still require a non-`SAFE` risk and one non-null swap.
  </behavior>
  <action>Create `tests/unit/revora/carbs-only-floor.test.ts` as a focused service-level regression that injects a carbs-only model output with sequencing-only wording and asserts the returned adjustment comes from the deterministic fallback copy, while the disclaimer and swap remain intact. In `tests/evals/revora-safety-eval.test.ts`, strengthen the carbs-only loop to reject final adjustments that only say `eat vegetables first`, `start with vegetables`, `before the carbs`, or equivalent sequencing-only phrasing, while still requiring add/pair-with protein or nonstarchy vegetables and a non-null swap. Do not weaken the existing zero harmful SAFE gate or broader result-contract assertions.</action>
  <verify>
    <automated>node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/carbs-only-floor.test.ts tests/evals/revora-safety-eval.test.ts -t "carbs-only"</automated>
  </verify>
  <done>Service-level regressions and eval assertions both fail if sequencing-only carbs-only guidance reappears in final user-facing output.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/postprocess.test.ts -t "carbs-only|sequencing|adjustment"` passes.
- [ ] `node scripts/validate-safety-contract.mjs && npx vitest run tests/unit/revora/carbs-only-floor.test.ts tests/evals/revora-safety-eval.test.ts -t "carbs-only"` passes.
- [ ] Carbs-only final responses still keep one non-null swap and never return `SAFE` in the targeted eval run.
- [ ] No new prompt-only workaround, hosted eval upload path, or broad classifier/parser was added.
</verification>

<success_criteria>
- Sequencing-only carbs-only guidance is rejected or deterministically floored before final rendering.
- The final carbs-only adjustment always tells the user to add or pair with protein or nonstarchy vegetables.
- Targeted unit and eval coverage catches future regressions even if the raw model wording changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-05-SUMMARY.md`
</output>
