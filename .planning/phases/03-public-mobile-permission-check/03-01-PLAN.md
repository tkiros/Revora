---
phase: 03-public-mobile-permission-check
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - app/page.tsx
  - components/food-check-form.tsx
  - lib/client/check.ts
  - lib/client/validation.ts
  - tests/unit/client/validation.test.ts
  - tests/smoke/mobile-check.spec.ts
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - UX-01
  - UX-02
  - UX-03
user_setup: []
must_haves:
  truths:
    - "A public user can open one mobile-first page with no login and enter a food description plus a one-decimal A1C value."
    - "Invalid or incomplete food/A1C input is blocked locally before any network request is sent."
    - "The page does not autofocus into a mobile keyboard trap on load."
    - "The primary CTA reads `Should I eat this?` and is reachable at mobile widths."
  artifacts:
    - path: "app/page.tsx"
      provides: "Single-screen public page shell for the Revora check"
      contains: "Should I eat this?"
    - path: "components/food-check-form.tsx"
      provides: "Client form with food/A1C inputs, inline validation, and CTA"
      contains: "inputMode=\"decimal\""
    - path: "lib/client/validation.ts"
      provides: "String-backed food/A1C validation seam for client and tests"
      exports: ["validateCheckForm"]
    - path: "lib/client/check.ts"
      provides: "Thin typed client wrapper to `POST /api/check`"
      exports: ["submitCheck"]
    - path: "tests/unit/client/validation.test.ts"
      provides: "Food/A1C validation coverage before any request logic"
      contains: "one decimal"
    - path: "tests/smoke/mobile-check.spec.ts"
      provides: "Public no-login mobile smoke harness for the single-screen form"
      contains: "public no-login form"
    - path: "playwright.config.ts"
      provides: "Mobile Chrome and Mobile Safari smoke configuration"
      contains: "Mobile Chrome"
  key_links:
    - from: "components/food-check-form.tsx"
      to: "lib/client/validation.ts"
      via: "Submit path validates trimmed food and one-decimal A1C before any fetch"
      pattern: "validateCheckForm"
    - from: "components/food-check-form.tsx"
      to: "lib/client/check.ts"
      via: "Valid form data is passed through the typed `/api/check` client seam"
      pattern: "submitCheck"
    - from: "app/page.tsx"
      to: "components/food-check-form.tsx"
      via: "Public page composes the form inline with no modal or navigation detour"
      pattern: "FoodCheckForm"
    - from: "tests/smoke/mobile-check.spec.ts"
      to: "app/page.tsx"
      via: "Playwright asserts public access, CTA label, invalid-submit blocking, and no autofocus"
      pattern: "Should I eat this"
---

<objective>
Build the single-screen mobile form and local validation flow.

Purpose: Phase 3 starts by turning the Phase 2 placeholder page into a real public form without weakening the single-route server contract.
Output: Public page shell, mobile-first food/A1C form, client validation seam, typed `/api/check` wrapper, Playwright mobile smoke config, and unit coverage for local validation.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-public-mobile-permission-check/03-RESEARCH.md
@.planning/phases/03-public-mobile-permission-check/03-VALIDATION.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-03-PLAN.md

<interfaces>
Phase 3 is a delivery shell over the Phase 2 contract. Before any UI edits, verify that Phase 2 execution has already produced these artifacts:

```text
app/page.tsx
app/api/check/route.ts
lib/revora/service.ts
lib/revora/schemas.ts
vitest.config.ts
```

If any of them are missing, stop and report that Phase 2 has not been executed; do not rebuild the inference stack in Phase 3.

Use the Phase 2 request/response boundary directly:

```typescript
export type RevoraRisk = "SAFE" | "MODERATE" | "HIGH";
export type RevoraResponseKind =
  | "result"
  | "clarify"
  | "not_food"
  | "out_of_scope"
  | "retry";

export type CheckRequest = {
  food: string;
  a1c: number;
};
```

Create these client-side contracts for later plans:

```typescript
export type CheckFormInput = {
  food: string;
  a1c: string;
};

export function validateCheckForm(
  input: CheckFormInput
): { ok: true; data: CheckRequest } | { ok: false; issues: Array<{ field: "food" | "a1c"; message: string }> };

export async function submitCheck(
  input: CheckRequest,
  init?: { signal?: AbortSignal }
): Promise<unknown>;
```

Do not import OpenAI, prompt helpers, safety policy modules, auth, saved history, payments, scanner logic, or any second classifier into `app/`, `components/`, or `lib/client/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 - verify Phase 2 artifacts and create client validation/smoke seams</name>
  <files>package.json, package-lock.json, playwright.config.ts, lib/client/validation.ts, lib/client/check.ts, tests/unit/client/validation.test.ts, tests/smoke/mobile-check.spec.ts</files>
  <behavior>
    - Test 1: `validateCheckForm()` accepts `{ food: "lentil soup", a1c: "6.1" }` and rejects empty food, empty A1C, nonnumeric A1C, multiple decimal points, and more than one decimal place.
    - Test 2: The initial smoke suite covers `public no-login form`, `invalid submit does not POST`, `cta label and position`, and `no autofocus mobile inputs`.
    - Test 3: `submitCheck()` is a thin client wrapper to `POST /api/check`; it does not import OpenAI or bypass the server route.
  </behavior>
  <action>Before any edits, verify that Phase 2 execution produced `app/page.tsx`, `app/api/check/route.ts`, `lib/revora/service.ts`, `lib/revora/schemas.ts`, and `vitest.config.ts`. If any are missing, stop and report the dependency failure instead of recreating them in Phase 3. If Playwright is absent from `package.json`, add `@playwright/test` and update `package-lock.json`. Create `playwright.config.ts` with `Mobile Chrome` and `Mobile Safari` projects plus a local `webServer` command suitable for the Next scaffold produced in Phase 2. Write `tests/unit/client/validation.test.ts` and the initial `tests/smoke/mobile-check.spec.ts` first. Implement `lib/client/validation.ts` with string-backed form validation and parsing that returns typed issues instead of throwing. Implement `lib/client/check.ts` as a thin typed `fetch("/api/check")` wrapper only; leave slow-state timers and friendly retry mapping for Plan 03-02. Do not edit `app/api/check/route.ts` here.</action>
  <verify>
    <automated>test -f app/page.tsx && test -f app/api/check/route.ts && test -f lib/revora/service.ts && test -f lib/revora/schemas.ts && test -f vitest.config.ts</automated>
    <automated>npx vitest run tests/unit/client/validation.test.ts</automated>
    <automated>rg -n "public no-login form|invalid submit does not POST|cta label and position|no autofocus mobile inputs" tests/smoke/mobile-check.spec.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Phase 2 artifacts are verified in place, the validation seam and initial smoke harness exist, and local validation tests plus smoke-fixture grep checks pass without adding any new inference path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Replace the placeholder page with the single-screen public mobile form</name>
  <files>app/page.tsx, components/food-check-form.tsx, lib/client/validation.ts, lib/client/check.ts, tests/unit/client/validation.test.ts, tests/smoke/mobile-check.spec.ts</files>
  <behavior>
    - Test 1: The page renders one public flow with no modal, account gate, or navigation branch.
    - Test 2: The food field and A1C field stay on the same screen, and the A1C field uses `type="number"`, `inputMode="decimal"`, `step="0.1"`, and no `autoFocus`.
    - Test 3: Invalid submit shows inline field errors and never calls `submitCheck()`.
    - Test 4: The primary button text is exactly `Should I eat this?`.
  </behavior>
  <action>Replace the Phase 2 placeholder `app/page.tsx` with a public mobile-first page that composes a client `FoodCheckForm` and keeps the explanation copy, inputs, validation feedback, and future response area on one scroll surface. Implement `components/food-check-form.tsx` with a text food field, a string-backed A1C field, large touch targets, inline validation messaging, `enterKeyHint="go"`, and no page-load autofocus. Wire submit through `validateCheckForm()` and the `submitCheck()` seam, but keep the async state orchestration minimal here so Plan 03-02 can own loading/slow/error behavior cleanly. Preserve the hard scope: no scanner, auth, saved history, database personalization, payments, OpenAI calls, or a second classifier.</action>
  <verify>
    <automated>npx vitest run tests/unit/client/validation.test.ts && npx playwright test tests/smoke/mobile-check.spec.ts -g "public no-login form|invalid submit does not POST|cta label and position|no autofocus mobile inputs" --project="Mobile Chrome"</automated>
    <automated>npm run typecheck</automated>
    <automated>! rg -n "autoFocus|autofocus" app/page.tsx components/food-check-form.tsx</automated>
  </verify>
  <done>The public page is a real one-screen mobile form, invalid input is blocked before fetch, the CTA label is exact, and mobile keyboard hints are in place without autofocus regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Phase 2 artifacts exist and were verified before UI work started.
- [ ] `npx vitest run tests/unit/client/validation.test.ts` passes.
- [ ] `npx playwright test tests/smoke/mobile-check.spec.ts -g "public no-login form|invalid submit does not POST|cta label and position|no autofocus mobile inputs" --project="Mobile Chrome"` passes.
- [ ] `npm run typecheck` passes.
- [ ] `! rg -n "from ['\\\"]openai|responses\\.create|buildRevora" app components lib/client` confirms the client shell did not rebuild Phase 2.
</verification>

<success_criteria>
- A public user can enter food text and a one-decimal A1C from one mobile-first page without login.
- Local validation blocks empty or malformed input before any model call.
- The CTA is a large, thumb-reachable `Should I eat this?` button.
- The page avoids autofocus and mobile keyboard traps.
- Playwright and Vitest seams exist for the rest of Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-mobile-permission-check/03-01-SUMMARY.md`
</output>
