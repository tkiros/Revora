---
phase: 03-public-mobile-permission-check
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/page.tsx
  - components/food-check-form.tsx
  - components/request-status.tsx
  - components/result-card.tsx
  - lib/client/check.ts
  - lib/client/ui-state.ts
  - tests/unit/client/ui-state.test.ts
  - tests/smoke/mobile-check.spec.ts
autonomous: true
requirements:
  - GUIDE-08
  - UX-04
  - UX-05
  - UX-06
user_setup: []
must_haves:
  truths:
    - "Submitting a valid check shows immediate loading feedback on the same page."
    - "A normal mocked response reaches a useful terminal state before the 5-second slow threshold."
    - "If the request runs longer than 5 seconds, the UI switches to clear still-running copy without dropping the request."
    - "Successful `result`, `clarify`, `not_food`, and `out_of_scope` responses render as useful inline states instead of raw JSON."
    - "Transport failures, timeouts, `429` responses, and server `retry` responses show friendly retry copy and never leak raw errors."
    - "The public shell still uses the single Phase 2 `/api/check` route rather than a second classifier."
  artifacts:
    - path: "lib/client/ui-state.ts"
      provides: "Request-state machine and friendly failure mapping for the mobile page"
      exports: ["CheckUiState", "mapCheckFailure", "isSlowThresholdReached"]
    - path: "components/request-status.tsx"
      provides: "Inline loading and slow-state surface for the form"
      contains: "still checking"
    - path: "components/result-card.tsx"
      provides: "Inline renderer for `result`, `clarify`, `not_food`, `out_of_scope`, and `retry` response kinds"
      contains: "SAFE"
    - path: "tests/unit/client/ui-state.test.ts"
      provides: "Slow-state and retry-mapping coverage outside React"
      contains: "five seconds"
    - path: "tests/smoke/mobile-check.spec.ts"
      provides: "Mobile smoke coverage for useful responses, slow state, and friendly retry states"
      contains: "friendly retry states"
  key_links:
    - from: "components/food-check-form.tsx"
      to: "lib/client/check.ts"
      via: "Validated input is submitted only through the typed `/api/check` wrapper"
      pattern: "submitCheck"
    - from: "components/food-check-form.tsx"
      to: "lib/client/ui-state.ts"
      via: "Pending, slow, success, and retry transitions come from the shared request-state helpers"
      pattern: "CheckUiState"
    - from: "components/request-status.tsx"
      to: "components/food-check-form.tsx"
      via: "Loading and still-running copy are rendered from the form's request state"
      pattern: "RequestStatus"
    - from: "components/result-card.tsx"
      to: "lib/client/check.ts"
      via: "Normalized server response kinds drive result, clarify, not-food, out-of-scope, and retry rendering"
      pattern: "kind"
---

<objective>
Connect the public API route to result, clarification, and error states.

Purpose: The public form is only useful once it renders Phase 2 responses, loading/slow feedback, and safe retry behavior without exposing raw backend errors.
Output: Client request-state seam, typed route integration, inline loading/slow UI, result/clarification/error components, and smoke coverage for normal, slow, and retry outcomes.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-public-mobile-permission-check/03-RESEARCH.md
@.planning/phases/03-public-mobile-permission-check/03-VALIDATION.md
@.planning/phases/03-public-mobile-permission-check/03-01-SUMMARY.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-PLAN.md

<interfaces>
Preserve the Plan 03-01 client seams:

```typescript
export type CheckFormInput = {
  food: string;
  a1c: string;
};

export function validateCheckForm(
  input: CheckFormInput
): { ok: true; data: { food: string; a1c: number } } | { ok: false; issues: Array<{ field: "food" | "a1c"; message: string }> };

export async function submitCheck(
  input: { food: string; a1c: number },
  init?: { signal?: AbortSignal }
): Promise<unknown>;
```

Preserve the Phase 2 response kinds. If exact field names drift from the plan, adapt only in `lib/client/check.ts`; do not edit server prompt, OpenAI, or policy code from the UI plan.

```typescript
export type RevoraResponseKind =
  | "result"
  | "clarify"
  | "not_food"
  | "out_of_scope"
  | "retry";

type RevoraUserResponse =
  | { kind: "result"; risk: "SAFE" | "MODERATE" | "HIGH"; reason: string; adjustment: string | null; swap: string | null; disclaimer: string }
  | { kind: "clarify"; question: string; disclaimer: string }
  | { kind: "not_food"; examples: string[]; disclaimer: string }
  | { kind: "out_of_scope"; reason: string; disclaimer: string }
  | { kind: "retry"; message: string; disclaimer: string };
```

Create this request-state contract before wiring React:

```typescript
export type CheckUiState =
  | { kind: "idle" }
  | { kind: "invalid"; message: string }
  | { kind: "submitting" }
  | { kind: "slow" }
  | { kind: "done"; response: RevoraUserResponse }
  | { kind: "error"; message: string };
```

Do not import OpenAI or Phase 2 prompt helpers into `app/`, `components/`, or `lib/client/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 - add request-state helpers and unit coverage for slow/retry behavior</name>
  <files>lib/client/ui-state.ts, tests/unit/client/ui-state.test.ts, tests/smoke/mobile-check.spec.ts</files>
  <behavior>
    - Test 1: `CheckUiState` transitions support `idle -> submitting -> slow -> done|error`.
    - Test 2: A slow threshold is recognized at 5 seconds without converting the request into an error state.
    - Test 3: Timeout, `429`, network failure, and server `retry` outcomes map to friendly retry copy rather than raw transport text.
    - Test 4: The smoke suite can route `/api/check` to mocked `result`, `clarify`, `retry`, slow, and `429` outcomes, including a normal-response fixture for a terminal state before 5 seconds.
  </behavior>
  <action>Create `lib/client/ui-state.ts` as the non-React seam for slow-threshold timing and failure mapping. Write `tests/unit/client/ui-state.test.ts` first so the 5-second threshold and retry-copy expectations are explicit. Extend `tests/smoke/mobile-check.spec.ts` with reusable route stubs using `page.route("**/api/check", ...)` for success, clarify, retry, `429`, delayed responses, and a normal mocked response that will later assert a useful terminal state appears before 5 seconds. Keep this helper layer client-only; do not encode classifier policy or duplicate Phase 2 guardrails here.</action>
  <verify>
    <automated>npx vitest run tests/unit/client/ui-state.test.ts</automated>
    <automated>rg -n "useful response states|normal response before five seconds|slow state after five seconds|friendly retry states|page\.route" tests/smoke/mobile-check.spec.ts</automated>
  </verify>
  <done>The async state machine and smoke-route stubs exist as explicit seams before the page wiring changes; UI smoke tests are present but not executed until the UI wiring tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Wire the form to `/api/check` with loading, slow-state, and retry-safe status handling</name>
  <files>app/page.tsx, components/food-check-form.tsx, components/request-status.tsx, lib/client/check.ts, lib/client/ui-state.ts, tests/unit/client/ui-state.test.ts, tests/smoke/mobile-check.spec.ts</files>
  <behavior>
    - Test 1: Valid submit sets the CTA into a loading state and prevents double-submit until the request settles.
    - Test 2: After 5 seconds, the page shows still-running copy while the request remains active.
    - Test 3: Network failure, timeout, `429`, and server `retry` outcomes render friendly retry copy with no raw error text.
    - Test 4: The page still stays on one route and submits only to `/api/check`.
  </behavior>
  <action>Update `lib/client/check.ts` so it performs the real `fetch("/api/check")`, adds `Content-Type: application/json`, supports `AbortSignal.timeout(12000)`, and normalizes transport errors into the request-state helpers. Implement `components/request-status.tsx` as the inline loading/slow surface. Update `components/food-check-form.tsx` and `app/page.tsx` so valid submit starts the request, flips the CTA into loading text, triggers the 5-second slow-state message, and maps failures/rate limits into friendly retry copy. Keep everything on the same page. If the executed Phase 2 JSON fields differ from the planned shape, adapt the mapping in `lib/client/check.ts` only; do not edit `app/api/check/route.ts`, `lib/revora/*`, or any OpenAI code from this plan.</action>
  <verify>
    <automated>npx vitest run tests/unit/client/ui-state.test.ts && npx playwright test tests/smoke/mobile-check.spec.ts -g "loading state|slow state after five seconds|friendly retry states" --project="Mobile Chrome"</automated>
    <automated>npm run typecheck</automated>
    <automated>! rg -n "from ['\\\"]openai|responses\\.create|buildRevora" app components lib/client</automated>
  </verify>
  <done>Valid submits show loading immediately, slow requests display still-running copy after 5 seconds, and failures/rate limits are converted into friendly retry states without exposing raw backend errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render result, clarification, and safe error variants inline on the public page</name>
  <files>app/page.tsx, components/food-check-form.tsx, components/request-status.tsx, components/result-card.tsx, tests/smoke/mobile-check.spec.ts</files>
  <behavior>
    - Test 1: A normal mocked `result` response renders `SAFE`, `MODERATE`, or `HIGH` plus the server-provided reason and disclaimer before the 5-second slow threshold under normal conditions.
    - Test 2: `clarify`, `not_food`, and `out_of_scope` responses render useful inline copy instead of generic failure text.
    - Test 3: `retry` responses share the friendly retry surface rather than leaking transport details.
    - Test 4: The public page stays one-screen/one-route even when switching among response kinds.
  </behavior>
  <action>Implement `components/result-card.tsx` and the page-level render branches so the UI handles the Phase 2 response kinds inline: `result` shows risk label, reason, optional adjustment/swap, and disclaimer; `clarify` shows one question; `not_food` shows concrete food examples; `out_of_scope` shows the A1C scope reminder; `retry` shows calm retry copy. Update the smoke suite to cover a successful normal mocked result that reaches a terminal useful state before 5 seconds, plus at least one clarify or not-food response in addition to slow and retry states. Keep disclaimer rendering on every terminal branch and do not add a second route, modal, scanner, auth, saved history, or any client-side policy engine.</action>
  <verify>
    <automated>npx vitest run tests/unit/client/ui-state.test.ts && npx playwright test tests/smoke/mobile-check.spec.ts -g "normal response before five seconds|useful response states|loading state|slow state after five seconds|friendly retry states" --project="Mobile Chrome"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The public page renders useful result, clarification, and safe retry/error states from the single Phase 2 route without leaving the mobile screen.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run tests/unit/client/ui-state.test.ts` passes.
- [ ] `npx playwright test tests/smoke/mobile-check.spec.ts -g "normal response before five seconds|loading state|slow state after five seconds|friendly retry states|useful response states" --project="Mobile Chrome"` passes.
- [ ] `npm run typecheck` passes.
- [ ] `! rg -n "from ['\\\"]openai|responses\\.create|buildRevora" app components lib/client` confirms the UI still treats Phase 2 as a hard server boundary.
- [ ] A normal mocked response renders a useful terminal state before 5 seconds, and successful, clarify, not-food/out-of-scope, slow, retry, timeout, and `429` states all stay on one page.
</verification>

<success_criteria>
- Valid submissions reach the existing `/api/check` route, show a loading CTA immediately, and render a normal useful terminal response before the 5-second ceiling.
- Slow requests show still-running copy after 5 seconds instead of looking stalled.
- Result, clarify, not-food, out-of-scope, and retry states all render useful inline content.
- No raw transport or server errors are shown to the user.
- The client shell still relies on the Phase 2 route instead of creating a second inference path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-mobile-permission-check/03-02-SUMMARY.md`
</output>
