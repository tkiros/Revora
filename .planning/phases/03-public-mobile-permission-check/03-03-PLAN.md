---
phase: 03-public-mobile-permission-check
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/food-check-form.tsx
  - components/request-status.tsx
  - components/result-card.tsx
  - tests/smoke/mobile-check.spec.ts
autonomous: false
requirements:
  - GUIDE-08
  - UX-01
  - UX-03
  - UX-07
user_setup: []
must_haves:
  truths:
    - "Result text, status copy, and input labels stay high-contrast and readable on a phone in bright conditions."
    - "The single-screen public flow remains intact after the visual tuning pass."
    - "Loading, still-running, and retry messaging stay explicit in text and do not rely on color alone."
    - "A real-device spot check confirms no autofocus-on-load jump and acceptable bright-environment readability."
  artifacts:
    - path: "app/globals.css"
      provides: "Bright-environment visual tokens and base typography for the public mobile page"
      contains: "--"
    - path: "app/layout.tsx"
      provides: "Global stylesheet import and top-level shell support for the tuned mobile experience"
      contains: "globals.css"
    - path: "components/result-card.tsx"
      provides: "Readable, text-first terminal-state cards for SAFE/MODERATE/HIGH and other response kinds"
      contains: "uppercase"
    - path: "components/request-status.tsx"
      provides: "Readable latency and retry copy with non-color-only cues"
      contains: "Try again"
    - path: "tests/smoke/mobile-check.spec.ts"
      provides: "Smoke coverage for readability, single-screen flow, and CTA placement"
      contains: "result readability"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "Top-level shell imports the bright-environment visual system"
      pattern: "globals.css"
    - from: "app/page.tsx"
      to: "components/result-card.tsx"
      via: "Final page layout preserves readable result rendering within one mobile flow"
      pattern: "ResultCard"
    - from: "components/request-status.tsx"
      to: "tests/smoke/mobile-check.spec.ts"
      via: "Playwright asserts readable loading, still-running, and retry copy"
      pattern: "still checking"
---

<objective>
Tune mobile readability, latency messaging, and bright-environment UX.

Purpose: The public flow must feel usable in a kitchen, restaurant, or grocery aisle, not merely technically complete in desktop dev tools.
Output: Final mobile readability styling, tuned latency/retry copy, smoke coverage for bright-environment constraints, and one human verification checkpoint for real-device confidence.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-public-mobile-permission-check/03-RESEARCH.md
@.planning/phases/03-public-mobile-permission-check/03-VALIDATION.md
@.planning/phases/03-public-mobile-permission-check/03-02-SUMMARY.md

<interfaces>
Plan 03-02 should already have these render seams in place. Preserve their props/contracts while tuning presentation:

```typescript
export type CheckUiState =
  | { kind: "idle" }
  | { kind: "invalid"; message: string }
  | { kind: "submitting" }
  | { kind: "slow" }
  | { kind: "done"; response: RevoraUserResponse }
  | { kind: "error"; message: string };

export function RequestStatus(props: {
  state: Extract<CheckUiState, { kind: "submitting" | "slow" | "error" }>;
}): JSX.Element;

export function ResultCard(props: {
  response: RevoraUserResponse;
}): JSX.Element;
```

If `app/globals.css` is absent from the Phase 2/03-02 scaffold, create it here and import it from `app/layout.tsx`. Do not add dark-mode complexity, scanner flows, auth, saved history, payments, or new backend behavior in this polish plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply a bright-environment visual system and mobile-first readability pass</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css, components/food-check-form.tsx, components/result-card.tsx</files>
  <action>Create or refine `app/globals.css` and import it from `app/layout.tsx` if needed. Apply a text-first, daylight-readable visual system to the public page: high-contrast dark text on a light background, explicit borders instead of low-contrast fills, minimum 16px body copy, larger result copy for terminal states, tap targets at least 44px tall, and spacing that keeps the CTA reachable without a second route or modal. Tune `app/page.tsx`, `components/food-check-form.tsx`, and `components/result-card.tsx` so SAFE/MODERATE/HIGH and other labels are explicit in text and never conveyed by color alone. Keep the screen visually calm and mobile-first; do not add dark-mode branching or generic UI-kit chrome.</action>
  <verify>
    <automated>npx playwright test tests/smoke/mobile-check.spec.ts -g "single screen flow|cta label and position|result readability" --project="Mobile Chrome"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The public flow remains one-screen and visually readable on a phone, with high-contrast text and non-color-only state cues.</done>
</task>

<task type="auto">
  <name>Task 2: Tune latency and retry copy, then lock the final smoke coverage</name>
  <files>app/page.tsx, components/request-status.tsx, components/result-card.tsx, tests/smoke/mobile-check.spec.ts</files>
  <action>Refine the loading, still-running, timeout, rate-limit, and general retry copy so it stays calm, specific, and permission-first. Keep the 5-second still-running message distinct from the final retry/error state, and ensure retry copy never exposes raw error strings. Expand `tests/smoke/mobile-check.spec.ts` so the final mobile suite asserts readable terminal states, one-page flow, exact CTA label, and the text content for loading/slow/retry surfaces on both mocked success and failure branches. Use Playwright assertions only; do not add a second testing framework or route-level backend logic here.</action>
  <verify>
    <automated>npx vitest run tests/unit/client/validation.test.ts tests/unit/client/ui-state.test.ts</automated>
    <automated>npx playwright test tests/smoke/mobile-check.spec.ts --project="Mobile Chrome"</automated>
    <automated>npx playwright test tests/smoke/mobile-check.spec.ts -g "result readability|single screen flow|cta label and position" --project="Mobile Safari"</automated>
  </verify>
  <done>Latency and retry messaging are finalized, and the final Playwright coverage proves the one-page mobile flow remains readable and explicit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify real-device readability and keyboard behavior</name>
  <action>Pause after the automated mobile smoke suite passes so the user can confirm the final bright-environment readability and real-device keyboard behavior before the plan is marked complete.</action>
  <what-built>Final bright-environment mobile UX pass for the public Revora check</what-built>
  <how-to-verify>
    1. Start the app locally with the command documented by the scaffold, then open the public page on a real phone.
    2. Confirm the page does not autofocus into the keyboard on load.
    3. Tap the food field and A1C field; confirm the A1C keyboard supports decimal entry and the CTA remains easy to reach.
    4. Run one mocked or real check that renders a terminal state; verify the result text, disclaimer, and retry/slow copy are readable in a bright room or near a bright window.
    5. If anything is hard to read or feels cramped, capture the issue in the plan summary before resuming.
  </how-to-verify>
  <verify>
    <automated>npx playwright test tests/smoke/mobile-check.spec.ts --project="Mobile Chrome"</automated>
  </verify>
  <done>User approves the real-device keyboard and readability pass, or records the issues that need correction before completion.</done>
  <resume-signal>Type "approved" if the page is readable and mobile-safe, or describe the issues that need correction.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run tests/unit/client/validation.test.ts tests/unit/client/ui-state.test.ts` passes.
- [ ] `npx playwright test tests/smoke/mobile-check.spec.ts --project="Mobile Chrome"` passes.
- [ ] `npx playwright test tests/smoke/mobile-check.spec.ts -g "result readability|single screen flow|cta label and position" --project="Mobile Safari"` passes.
- [ ] `npm run typecheck` passes.
- [ ] Manual real-device check confirms no autofocus-on-load jump and acceptable bright-environment readability.
</verification>

<success_criteria>
- The public mobile page remains single-screen, high-contrast, and readable in bright conditions.
- Loading, still-running, and retry states use explicit readable text and not color alone.
- Final smoke coverage passes on mobile-oriented Playwright projects.
- A human verification pass signs off on real-device keyboard and readability behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-mobile-permission-check/03-03-SUMMARY.md`
</output>
