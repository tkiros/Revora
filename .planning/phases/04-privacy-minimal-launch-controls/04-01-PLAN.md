---
phase: 04-privacy-minimal-launch-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/privacy/data-flow.md
  - lib/revora/service.ts
  - lib/revora/openai-client.ts
  - lib/revora/telemetry.ts
  - lib/revora/env.ts
  - app/api/check/route.ts
  - app/api/health/route.ts
  - tests/unit/revora/privacy-minimal.test.ts
  - tests/unit/revora/openai-client.test.ts
  - tests/unit/revora/telemetry.test.ts
  - tests/unit/revora/env.test.ts
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - OPS-01
user_setup:
  - service: openai
    why: "Preview and Production deployments need the server-side Responses API key used by the single OpenAI wrapper."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard API keys"
  - service: vercel
    why: "Preview and Production deployment, environment scoping, and later launch controls live in Vercel."
    dashboard_config:
      - task: "Create separate Preview and Production environment-variable scopes for Revora"
        location: "Vercel Project -> Settings -> Environment Variables"
must_haves:
  truths:
    - "Revora can be built for Vercel without introducing default storage of raw food descriptions, raw A1C values, or account-linked health data."
    - "Every OpenAI Responses call used by Revora goes through one server-only wrapper that sets `store: false`."
    - "Telemetry and operational events carry allowlisted coarse fields only and exclude raw food, raw A1C, prompt text, and full model output."
    - "Preview and Production configuration are explicitly separated before public launch."
    - "A safe health and launch-state probe exists for Preview verification and later rollback checks."
    - "The no-default-storage boundary has dedicated executable proof that fails if raw request logging or direct persistence seams are introduced."
  artifacts:
    - path: "docs/privacy/data-flow.md"
      provides: "Documented raw-input lifetime, telemetry allowlist, provider-storage posture, and no-default-storage boundary"
      contains: "store: false"
    - path: "app/api/check/route.ts"
      provides: "Single public check route delegating to the server service seam and emitting coarse telemetry only"
      exports: ["POST"]
    - path: "lib/revora/service.ts"
      provides: "Single server-side checkFood seam preserving the route -> service -> wrapper path"
      exports: ["checkFood"]
    - path: "lib/revora/openai-client.ts"
      provides: "Single server-only OpenAI Responses wrapper with explicit storage opt-out"
      contains: "store: false"
    - path: "lib/revora/telemetry.ts"
      provides: "Allowlisted coarse telemetry builder and emitter with no raw health-adjacent fields"
      exports: ["emitSafeEvent"]
    - path: "lib/revora/env.ts"
      provides: "Preview and Production environment validation plus launch-time config contract"
      exports: ["getRevoraEnv"]
    - path: "app/api/health/route.ts"
      provides: "Safe JSON health and launch-state probe for Preview verification and rollback checks"
      exports: ["GET"]
    - path: "tests/unit/revora/privacy-minimal.test.ts"
      provides: "Dedicated no-default-storage audit over wrapper, route, and telemetry seams"
      contains: "raw A1C"
    - path: "tests/unit/revora/openai-client.test.ts"
      provides: "Explicit provider-storage opt-out and single-wrapper coverage"
      contains: "store false"
  key_links:
    - from: "app/api/check/route.ts"
      to: "lib/revora/service.ts"
      via: "The public check route must delegate to the existing `checkFood()` service seam instead of calling the SDK directly"
      pattern: "checkFood"
    - from: "lib/revora/service.ts"
      to: "lib/revora/openai-client.ts"
      via: "The single `checkFood()` path continues to own every model call through the hardened wrapper"
      pattern: "store: false"
    - from: "app/api/check/route.ts"
      to: "lib/revora/telemetry.ts"
      via: "The existing request path emits coarse operational events without logging raw request bodies"
      pattern: "emitSafeEvent"
    - from: "app/api/health/route.ts"
      to: "lib/revora/env.ts"
      via: "The safe probe reads validated environment and launch config without exposing secrets"
      pattern: "getRevoraEnv"
    - from: "docs/privacy/data-flow.md"
      to: "lib/revora/openai-client.ts"
      via: "Documentation and implementation share the same provider-storage and no-default-storage boundary"
      pattern: "store: false"
---

<objective>
Lock privacy-minimal data flow, telemetry, and deployment configuration.

Purpose: Revora cannot be launched publicly until the single Phase 2 and 3 request path has an auditable privacy boundary and Vercel deployment contract.
Output: Dependency gate evidence, privacy data-flow documentation, one hardened server-only OpenAI wrapper with `store: false`, allowlisted coarse telemetry, Preview and Production env validation, and a safe health probe for deployment checks.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-privacy-minimal-launch-controls/04-RESEARCH.md
@.planning/phases/04-privacy-minimal-launch-controls/04-VALIDATION.md
@.planning/phases/03-public-mobile-permission-check/03-01-PLAN.md
@.planning/phases/03-public-mobile-permission-check/03-02-PLAN.md
@.planning/phases/03-public-mobile-permission-check/03-03-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-01-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-02-PLAN.md
@.planning/phases/02-guardrailed-inference-core-and-eval-harness/02-03-PLAN.md

<interfaces>
Phase 4 is a hardening layer over the existing Phase 2 and 3 contracts. Before any edits, verify that execution of the earlier phases already produced these files:

```text
package.json
vitest.config.ts
playwright.config.ts
app/layout.tsx
app/globals.css
app/page.tsx
app/api/check/route.ts
components/request-status.tsx
components/result-card.tsx
lib/client/ui-state.ts
lib/revora/service.ts
lib/revora/openai-client.ts
tests/smoke/mobile-check.spec.ts
tests/unit/revora/service.test.ts
```

If any required artifact above is absent from the workspace, stop and report `BLOCKED_PHASE_DEPENDENCY`; do not rebuild the app scaffold, Phase 3 public flow, or inference stack inside Phase 4.

Preserve the single server-side request path from Phase 2:

```typescript
export async function checkFood(
  rawRequest: unknown,
  deps: { model: RevoraModelClient }
): Promise<RevoraUserResponse>;
```

Preserve the public API shape from Phases 2 and 3. Do not add a second route, client-side OpenAI access, auth, saved history, database writes, scanner flows, or rich analytics:

```typescript
export type RevoraResponseKind =
  | "result"
  | "clarify"
  | "not_food"
  | "out_of_scope"
  | "retry";
```

Phase 4 creates these new privacy contracts for later plans to consume:

```typescript
export type SafeTelemetryEvent = {
  name: "check_completed" | "check_failed" | "launch_probe";
  environment: "preview" | "production" | "development" | "test";
  responseKind?: "result" | "clarify" | "not_food" | "out_of_scope" | "retry";
  risk?: "SAFE" | "MODERATE" | "HIGH";
  latencyBucket?: "<2s" | "2-5s" | "5-12s" | ">12s";
  reasonCode?: "rate_limited" | "provider_error" | "schema_error" | "paused";
};

export function emitSafeEvent(event: SafeTelemetryEvent): void;

export function getRevoraEnv(input?: NodeJS.ProcessEnv): {
  environment: "preview" | "production" | "development" | "test";
  openAiApiKey: string;
  edgeConfigConnectionString?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Run the Phase 2 and 3 dependency gate, then lock the privacy boundary document</name>
  <files>docs/privacy/data-flow.md</files>
  <action>Run one explicit dependency gate before touching privacy or deployment code. Check for `package.json`, `vitest.config.ts`, `playwright.config.ts`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `app/api/check/route.ts`, `components/request-status.tsx`, `components/result-card.tsx`, `lib/client/ui-state.ts`, `lib/revora/service.ts`, `lib/revora/openai-client.ts`, `tests/smoke/mobile-check.spec.ts`, and `tests/unit/revora/service.test.ts`. If any required artifact is missing from the workspace, stop immediately, record `BLOCKED_PHASE_DEPENDENCY` in the plan summary, and do not recreate missing Phase 2 or 3 artifacts inside Phase 4. If the gate passes, create `docs/privacy/data-flow.md` that freezes the privacy contract: raw food and raw A1C live only inside the in-flight request and the one server-side model call; no auth, saved history, database persistence, or raw request-body logging is allowed; no prompt text or full model output may be logged or sent to analytics; `store: false` is required on every Responses call where supported; and the document must explicitly avoid overclaiming zero retention by noting that provider-side abuse-monitoring logs can still exist. Include the Preview versus Production env boundary and the safe health probe that later rollback verification will use.</action>
  <verify>
    <automated>test -f package.json && test -f vitest.config.ts && test -f playwright.config.ts && test -f app/layout.tsx && test -f app/globals.css && test -f app/page.tsx && test -f app/api/check/route.ts && test -f components/request-status.tsx && test -f components/result-card.tsx && test -f lib/client/ui-state.ts && test -f lib/revora/service.ts && test -f lib/revora/openai-client.ts && test -f tests/smoke/mobile-check.spec.ts && test -f tests/unit/revora/service.test.ts && rg -n "store: false|raw food|raw A1C|Preview|Production|abuse-monitoring" docs/privacy/data-flow.md</automated>
  </verify>
  <done>The execution path is blocked early if Phase 2 or 3 was never built, and the privacy-minimal data-flow contract is documented in one place before code hardening starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Harden the single OpenAI wrapper and coarse telemetry allowlist</name>
  <files>lib/revora/service.ts, lib/revora/openai-client.ts, lib/revora/telemetry.ts, app/api/check/route.ts, tests/unit/revora/privacy-minimal.test.ts, tests/unit/revora/openai-client.test.ts, tests/unit/revora/telemetry.test.ts</files>
  <behavior>
    - Test 1: Every `responses.create()` call from `lib/revora/openai-client.ts` sets `store: false`.
    - Test 2: There is still exactly one server-only OpenAI wrapper, and the public page or client code does not import the SDK directly.
    - Test 3: Telemetry accepts only allowlisted coarse fields such as event name, environment, response kind, risk, latency bucket, and coarse reason code.
    - Test 4: Telemetry tests prove raw food text, raw A1C, prompt text, and full model output are not accepted or emitted.
    - Test 5: `tests/unit/revora/privacy-minimal.test.ts` statically audits the full request path `app/api/check/route.ts -> lib/revora/service.ts -> lib/revora/openai-client.ts` plus `lib/revora/telemetry.ts` for no default-storage seams, no direct SDK calls outside the wrapper, and no raw request logging.
    - Test 6: The API route emits coarse success or failure events after the `checkFood()` result is known and does not log raw request bodies, prompt text, or full model output.
  </behavior>
  <action>Write or update the unit tests first, including a dedicated `tests/unit/revora/privacy-minimal.test.ts` seam that statically audits `app/api/check/route.ts -> lib/revora/service.ts -> lib/revora/openai-client.ts` and `lib/revora/telemetry.ts` for the no-default-storage boundary. Preserve `lib/revora/service.ts` as the single service seam between the route and the OpenAI wrapper; do not collapse the path or bypass it from the route. Then harden `lib/revora/openai-client.ts` so every Responses API call used by Revora stays inside the one server-only wrapper and always sets `store: false`; do not scatter direct SDK calls into routes, `lib/revora/service.ts`, components, or utilities. Implement `lib/revora/telemetry.ts` as an allowlisted telemetry seam that builds events from safe fields only. Use it from `app/api/check/route.ts` to emit coarse operational events such as completion, retry, and failure categories after `checkFood()` returns, without logging raw request bodies, prompt text, or full model outputs. If the existing route or helper code serializes raw errors or raw inputs, replace that behavior with coarse reason codes. Do not add third-party analytics SDKs, session replay, user IDs, account IDs, raw health data, or a second classifier path.</action>
  <verify>
    <automated>npx vitest run tests/unit/revora/privacy-minimal.test.ts tests/unit/revora/openai-client.test.ts tests/unit/revora/telemetry.test.ts</automated>
    <automated>rg -n "store: false" lib/revora/openai-client.ts && rg -n "checkFood" app/api/check/route.ts lib/revora/service.ts && rg -n "openai-client" lib/revora/service.ts && rg -n "emitSafeEvent" app/api/check/route.ts && ! rg -n "from ['\\\"]openai" app/page.tsx components lib/client app/api/check/route.ts lib/revora/service.ts</automated>
  </verify>
  <done>The single OpenAI path is auditable for `store: false`, and the request path now emits privacy-minimal operational events without leaking raw food, raw A1C, prompt text, or full model output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Preview and Production env validation plus a safe health probe</name>
  <files>lib/revora/env.ts, app/api/health/route.ts, tests/unit/revora/env.test.ts, docs/privacy/data-flow.md</files>
  <behavior>
    - Test 1: Environment parsing distinguishes Preview, Production, Development, and Test, and it fails clearly when required server vars are missing.
    - Test 2: `OPENAI_API_KEY` is required for build and runtime use, while the later Edge Config connection string is optional until Plan 04-02.
    - Test 3: `/api/health` returns minimal JSON such as environment and launch-state status without exposing secrets, raw inputs, prompt text, or model output.
    - Test 4: `npm run typecheck`, `npm run build`, and `npx vercel build` succeed once the env contract is satisfied.
  </behavior>
  <action>Write `tests/unit/revora/env.test.ts` first, then implement `lib/revora/env.ts` with a small, explicit env parser that validates `OPENAI_API_KEY`, identifies the runtime environment, and documents the future `EDGE_CONFIG` requirement without making it mandatory until Plan 04-02. Add `app/api/health/route.ts` as a safe probe for Preview verification and later rollback drills; it should expose only non-secret launch metadata such as `ok`, `environment`, and a default launch-state status. Update `docs/privacy/data-flow.md` so the documented Vercel env separation matches the code contract exactly. Do not add database config, account config, or a second deployment path.</action>
  <verify>
    <automated>npx vitest run tests/unit/revora/env.test.ts</automated>
    <automated>npm run typecheck && npm run build && npx vercel build</automated>
    <automated>rg -n "environment|launch" app/api/health/route.ts</automated>
  </verify>
  <done>Revora has an explicit Preview and Production env contract, a safe health probe for deployment verification, and a Vercel build path that does not rely on implicit or undocumented configuration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f package.json && test -f vitest.config.ts && test -f playwright.config.ts && test -f app/page.tsx && test -f app/api/check/route.ts && test -f lib/revora/service.ts && test -f lib/revora/openai-client.ts && test -f tests/unit/revora/service.test.ts` passes.
- [ ] The dependency gate proves the real Phase 3 public flow exists in the workspace through `app/layout.tsx`, `app/globals.css`, `components/request-status.tsx`, `components/result-card.tsx`, `lib/client/ui-state.ts`, and `tests/smoke/mobile-check.spec.ts`; if any are missing, the plan stops with `BLOCKED_PHASE_DEPENDENCY`.
- [ ] `npx vitest run tests/unit/revora/privacy-minimal.test.ts tests/unit/revora/openai-client.test.ts tests/unit/revora/telemetry.test.ts tests/unit/revora/env.test.ts` passes.
- [ ] `npm run typecheck && npm run build && npx vercel build` passes.
- [ ] `rg -n "store: false" lib/revora/openai-client.ts` confirms provider-side storage opt-out is explicit.
- [ ] `rg -n "checkFood" app/api/check/route.ts lib/revora/service.ts && rg -n "openai-client" lib/revora/service.ts && rg -n "emitSafeEvent" app/api/check/route.ts` confirms the audited request path remains `app/api/check/route.ts -> lib/revora/service.ts -> lib/revora/openai-client.ts` with the telemetry seam attached at the route.
- [ ] `! rg -n "console\\.log\\(.*(food|a1c)|response\\.output_text|prompt text" app/api/check/route.ts lib/revora/telemetry.ts` confirms raw food, raw A1C, prompt text, and full model output are not being emitted from the Phase 4 telemetry path.
- [ ] `rg -n "raw food|raw A1C|abuse-monitoring|Preview|Production" docs/privacy/data-flow.md` confirms the docs do not overclaim zero retention and do document env separation.
</verification>

<success_criteria>
- Revora still uses one server-side `checkFood()` path and one server-only OpenAI wrapper.
- `store: false` is explicit on every Responses call used by the app.
- No default storage of raw food, raw A1C, or account-linked health data is introduced.
- A dedicated privacy-minimal audit test fails if raw request logging or new persistence seams are introduced.
- Telemetry is limited to allowlisted coarse events and excludes raw food, raw A1C, prompt text, and full model output.
- Preview and Production config are explicit, buildable, and verifiable through a safe health probe.
</success_criteria>

<output>
After completion, create `.planning/phases/04-privacy-minimal-launch-controls/04-01-SUMMARY.md`
</output>
