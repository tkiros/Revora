---
phase: 04-privacy-minimal-launch-controls
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - package.json
  - package-lock.json
  - docs/ops/launch-controls.md
  - lib/revora/launch-controls.ts
  - middleware.ts
  - app/api/health/route.ts
  - tests/unit/revora/launch-controls.test.ts
  - tests/smoke/launch-controls.spec.ts
autonomous: true
requirements:
  - OPS-02
  - OPS-03
user_setup:
  - service: vercel
    why: "Edge Config, WAF rule publication, Preview and Production deploys, and rollback all depend on the Vercel project."
    env_vars:
      - name: EDGE_CONFIG
        source: "Vercel Edge Config connection string"
    dashboard_config:
      - task: "Create Edge Config keys `launch_mode`, `public_checks_enabled`, and `incident_message`"
        location: "Vercel Dashboard -> Storage -> Edge Config"
      - task: "Publish a WAF rate-limit rule for `/api/check`"
        location: "Vercel Dashboard -> Security -> WAF"
must_haves:
  truths:
    - "A pause switch can disable public checks without redeploying."
    - "Paused or rate-limited checks fail before model spend and show friendly pause or retry copy instead of raw errors."
    - "Launch thresholds define when to rate-limit, when to pause, and when rollback is required."
    - "Rollback steps are documented with post-rollback verification instead of assuming rollback equals recovery."
    - "Provider-side verification evidence to collect for Preview, WAF, Edge Config, and rollback is explicit."
  artifacts:
    - path: "lib/revora/launch-controls.ts"
      provides: "Launch-mode evaluation, Edge Config reads, threshold helpers, and test-only non-production override seam"
      exports: ["getLaunchControls", "evaluateLaunchMode", "shouldPauseForOps"]
    - path: "middleware.ts"
      provides: "Pre-model pause gate for the public check path"
      contains: "public_checks_enabled"
    - path: "tests/unit/revora/launch-controls.test.ts"
      provides: "Threshold, pause, and missing-config coverage for launch controls"
      contains: "2,000"
    - path: "tests/smoke/launch-controls.spec.ts"
      provides: "Public smoke coverage for maintenance mode and friendly rate-limit behavior"
      contains: "maintenance mode"
    - path: "docs/ops/launch-controls.md"
      provides: "Threshold table, WAF and Edge Config setup, rollback drill, and post-rollback verification evidence"
      contains: "vercel rollback"
    - path: "app/api/health/route.ts"
      provides: "Safe health and launch-state probe wired to the real launch-control seam"
      contains: "launchMode"
  key_links:
    - from: "middleware.ts"
      to: "lib/revora/launch-controls.ts"
      via: "The public check path is paused before model spend using the shared launch-control evaluation"
      pattern: "evaluateLaunchMode"
    - from: "app/api/health/route.ts"
      to: "lib/revora/launch-controls.ts"
      via: "The health probe reports the same launch state used by the pause gate"
      pattern: "getLaunchControls"
    - from: "tests/smoke/launch-controls.spec.ts"
      to: "middleware.ts"
      via: "Playwright proves maintenance-mode and rate-limit behavior through the public path"
      pattern: "maintenance mode"
    - from: "docs/ops/launch-controls.md"
      to: "lib/revora/launch-controls.ts"
      via: "The runbook uses the same Edge Config keys and threshold constants implemented in code"
      pattern: "public_checks_enabled"
---

<objective>
Add abuse-cost thresholds plus rollback and kill-switch procedures.

Purpose: Public launch is unsafe until Revora can pause expensive or harmful traffic quickly, document operator thresholds clearly, and verify recovery after rollback.
Output: Launch-control helper seam, `@vercel/edge-config` integration, pre-model pause gate, health probe wired to launch state, smoke coverage for maintenance mode, and an operator runbook for WAF, Edge Config, rollback, and post-rollback verification.
</objective>

<execution_context>
@/home/tefera/.codex/get-shit-done/workflows/execute-plan.md
@/home/tefera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-privacy-minimal-launch-controls/04-RESEARCH.md
@.planning/phases/04-privacy-minimal-launch-controls/04-VALIDATION.md
@.planning/phases/04-privacy-minimal-launch-controls/04-01-SUMMARY.md
@.planning/phases/03-public-mobile-permission-check/03-02-PLAN.md
@.planning/phases/03-public-mobile-permission-check/03-03-PLAN.md

<interfaces>
This plan depends on Plan 04-01 having already produced these files. Verify them before any edits:

```text
docs/privacy/data-flow.md
lib/revora/openai-client.ts
lib/revora/telemetry.ts
lib/revora/env.ts
app/api/check/route.ts
app/api/health/route.ts
tests/unit/revora/openai-client.test.ts
tests/unit/revora/telemetry.test.ts
tests/unit/revora/env.test.ts
```

If any are missing, stop and report `BLOCKED_PLAN_04_01`; do not recreate the privacy boundary inside this plan.

Preserve the Phase 4 privacy seam from Plan 04-01:

```typescript
export type SafeTelemetryEvent = {
  name: "check_completed" | "check_failed" | "launch_probe";
  environment: "preview" | "production" | "development" | "test";
  responseKind?: "result" | "clarify" | "not_food" | "out_of_scope" | "retry";
  risk?: "SAFE" | "MODERATE" | "HIGH";
  latencyBucket?: "<2s" | "2-5s" | "5-12s" | ">12s";
  reasonCode?: "rate_limited" | "provider_error" | "schema_error" | "paused";
};
```

Create these launch-control contracts before wiring middleware:

```typescript
export type LaunchMode = "normal" | "paused";

export type LaunchControls = {
  launchMode: LaunchMode;
  publicChecksEnabled: boolean;
  incidentMessage: string;
};

export async function getLaunchControls(): Promise<LaunchControls>;
export async function evaluateLaunchMode(): Promise<
  | { ok: true; controls: LaunchControls }
  | { ok: false; status: 503; message: string; controls: LaunchControls }
>;
export function shouldPauseForOps(input: {
  checksLast24h: number;
  harmfulGuidanceIncident: boolean;
  providerFailureSpike: boolean;
}): boolean;
```

Use Vercel WAF plus operator thresholds and Edge Config pause as the default abuse and cost response. Do not add Upstash, Redis, client-only throttling, auth, raw request logging, or a second classifier unless the summary proves WAF plus operator pause is insufficient.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Verify Plan 04-01 outputs, then define launch-control contracts and thresholds</name>
  <files>package.json, package-lock.json, docs/ops/launch-controls.md, lib/revora/launch-controls.ts, tests/unit/revora/launch-controls.test.ts</files>
  <behavior>
    - Test 1: The plan stops if any required 04-01 privacy artifacts are missing.
    - Test 2: `getLaunchControls()` defaults to normal mode with public checks enabled when no pause state is configured.
    - Test 3: `evaluateLaunchMode()` returns a friendly paused response when `public_checks_enabled` is false.
    - Test 4: `shouldPauseForOps()` returns `true` for harmful-guidance incidents, provider-failure spikes, or `checksLast24h >= 2000`.
    - Test 5: Missing Edge Config values fail closed to safe defaults or clear operator messages instead of throwing raw provider errors.
  </behavior>
  <action>Run a dependency gate for the 04-01 artifacts listed in the interface block and stop with `BLOCKED_PLAN_04_01` if any are missing. Write `tests/unit/revora/launch-controls.test.ts` first. Then add `@vercel/edge-config` to `package.json` and `package-lock.json`, implement `lib/revora/launch-controls.ts`, and seed `docs/ops/launch-controls.md`. The code should read Edge Config keys `launch_mode`, `public_checks_enabled`, and `incident_message`, while also supporting a non-production test override such as `REVORA_LAUNCH_MODE_OVERRIDE=paused` so unit and smoke tests can simulate incidents without mutating live provider state. Encode the operator threshold from the project constraints: document WAF as `10 requests / 10 minutes / IP` for `/api/check`, and encode `2,000 checks / 24h` plus harmful-guidance incidents or provider-failure spikes as the pause threshold. Do not add Upstash, Redis, durable counters, or client-only throttling in this default plan.</action>
  <verify>
    <automated>test -f docs/privacy/data-flow.md && test -f lib/revora/openai-client.ts && test -f lib/revora/telemetry.ts && test -f lib/revora/env.ts && test -f app/api/check/route.ts && test -f app/api/health/route.ts && test -f tests/unit/revora/openai-client.test.ts && test -f tests/unit/revora/telemetry.test.ts && test -f tests/unit/revora/env.test.ts && npx vitest run tests/unit/revora/launch-controls.test.ts -t "default|pause|threshold|missing"</automated>
    <automated>rg -n "10 requests / 10 minutes|2,000 checks / 24h|public_checks_enabled|incident_message" docs/ops/launch-controls.md lib/revora/launch-controls.ts</automated>
  </verify>
  <done>The launch-control seam exists with explicit threshold helpers and Edge Config keys, and the plan still fails early if 04-01 never executed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Gate public checks before model spend and prove maintenance-mode behavior</name>
  <files>middleware.ts, app/api/health/route.ts, lib/revora/launch-controls.ts, tests/unit/revora/launch-controls.test.ts, tests/smoke/launch-controls.spec.ts</files>
  <behavior>
    - Test 1: When launch mode is normal, the public page and `/api/check` still work through the existing Phase 3 and Phase 2 flow.
    - Test 2: When `public_checks_enabled` is false or the test override is paused, the public check path returns a friendly 503 pause response before any model call.
    - Test 3: Maintenance-mode and rate-limit surfaces never leak raw provider errors, stack traces, raw food text, or prompt text.
    - Test 4: `/api/health` reports the same launch state used by the pause gate without exposing secrets.
    - Test 5: Playwright can cover normal and paused behavior deterministically without mutating live Edge Config.
  </behavior>
  <action>Implement the pre-model pause gate in `middleware.ts` for the public `/api/check` path using the shared `evaluateLaunchMode()` helper. Keep the public page accessible, but ensure paused checks fail before the OpenAI path is reached and return calm retry or incident copy that the existing Phase 3 UI can render safely. Update `app/api/health/route.ts` so it reports the real launch state from `lib/revora/launch-controls.ts`. Add `tests/smoke/launch-controls.spec.ts` using Playwright to prove maintenance-mode and friendly rate-limit behavior with the non-production override seam; keep the test public-path focused and do not introduce a second backend path or client-side OpenAI logic.</action>
  <verify>
    <automated>npx vitest run tests/unit/revora/launch-controls.test.ts -t "normal|pause|health"</automated>
    <automated>npx playwright test tests/smoke/launch-controls.spec.ts -g "maintenance mode|rate limit" --project="Mobile Chrome"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Revora can pause public checks before model spend, and both the health probe and smoke suite prove maintenance-mode behavior without leaking raw errors.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the rollback and kill-switch runbook with provider-side verification steps</name>
  <files>docs/ops/launch-controls.md, tests/smoke/launch-controls.spec.ts</files>
  <action>Finish `docs/ops/launch-controls.md` as the operator runbook. It must contain: the Preview deploy checklist and required evidence to capture; the WAF rule name, path matcher, limit, and publish state to record; the Edge Config keys and the exact pause and restore drill; and the rollback sequence with `vercel rollback`, `vercel rollback status`, `vercel logs --environment production --status-code 5xx --since 5m`, `GET /api/health`, and one synthetic public-check verification after restore. Require the executor to record exact evidence or mark the step `SETUP_BLOCKED` if CLI or provider auth is unavailable. Keep the runbook honest: rollback is not recovery until post-rollback health, logs, and one synthetic check are verified. Do not overclaim zero retention or invent provider-side automation that is not actually executed.</action>
  <verify>
    <automated>rg -n "vercel rollback|vercel rollback status|public_checks_enabled|incident_message|10 requests / 10 minutes|2,000 checks / 24h|SETUP_BLOCKED|/api/health" docs/ops/launch-controls.md</automated>
    <automated>npx playwright test tests/smoke/launch-controls.spec.ts -g "maintenance mode" --project="Mobile Chrome"</automated>
  </verify>
  <done>The operator runbook now captures exact pause, WAF, Preview, rollback, and post-rollback verification steps with evidence requirements instead of vague incident guidance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/privacy/data-flow.md && test -f lib/revora/openai-client.ts && test -f lib/revora/telemetry.ts && test -f lib/revora/env.ts && test -f app/api/check/route.ts && test -f app/api/health/route.ts` passes.
- [ ] `npx vitest run tests/unit/revora/launch-controls.test.ts` passes.
- [ ] `npx playwright test tests/smoke/launch-controls.spec.ts -g "maintenance mode|rate limit" --project="Mobile Chrome"` passes.
- [ ] `npm run typecheck` passes.
- [ ] `rg -n "10 requests / 10 minutes|2,000 checks / 24h|public_checks_enabled|vercel rollback|/api/health|SETUP_BLOCKED" docs/ops/launch-controls.md` confirms the runbook contains the required thresholds and evidence fields.
- [ ] Provider-side follow-up is prepared: Preview env verification, WAF publish verification, Edge Config toggle drill, and rollback drill each have a concrete command or evidence slot in the runbook.
</verification>

<success_criteria>
- Revora can be paused quickly without redeploying.
- Public checks fail before model spend when launch mode is paused.
- WAF and operator thresholds are explicit and aligned with the project cost constraint.
- Maintenance-mode and rate-limit behavior are covered by unit plus Playwright smoke tests.
- The rollback path is documented with post-rollback verification rather than assumed safe by default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-privacy-minimal-launch-controls/04-02-SUMMARY.md`
</output>
